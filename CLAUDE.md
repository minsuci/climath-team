# climath-team — 팀체크

**앱 이름은 «팀체크»다** (2026-09-07. 그 전엔 «한민수의 대시보드» — 팀이 같이 쓰는데 한 사람 이름이었다).
**저장소·Vercel 프로젝트·주소는 `climath-team` 그대로 둔다** — 주소를 바꾸면 선생님들 북마크가 죽고
볼트 링크·개발 현황 목록까지 다 따라가야 한다. 이름은 화면(`<title>`·머리띠)에만 있다.

대치 클라이매쓰 고등부 교무팀의 화면. **2026-09-05부터 선생님도 들어온다 — 전부 보되 읽기만.**
수업관리 앱(`climath-class`)과 **별개의 앱**이다. 저장소·Vercel·Firebase 프로젝트가 따로 있고, 앱 DB는 **읽기만** 한다.

```
repo/
├── index.html        # 팀체크 전체. 순수 JS, 빌드 없음
├── api/auth.js       # 로그인 — 수업관리 앱 /api/auth 에 PIN 확인을 맡기고 토큰 둘을 준다
├── api/sheets.js     # 구글시트 읽기 (서비스 계정, 읽기 전용)
├── api/github.js     # 깃허브 커밋·배포 이력 읽기 — «개발 현황» (5분 캐시)
├── tools/push-devlog.mjs  # 나스 허브의 도구보고·일지 개발 줄 → devtools·devlog (개발 현황의 나스 쪽)
├── tools/check-rules.mjs  # 게시된 규칙이 실제로 막는지 — 선생님 토큰으로 불러 본다
├── api/_google.js    # 커스텀 토큰 발급 · ID 토큰 검증 · 구글 API 토큰
├── firestore.rules   # climath-team DB 규칙 — 읽기 owner·teacher, 쓰기 owner (+ marks/<tid> 는 본인, minutes 는 open 인 것만 teacher). 콘솔에 붙여넣어 게시 (tools/publish-rules.mjs 는 권한이 없어 403)
└── vercel.json       # icn1
```

- 배포: https://climath-team1.vercel.app — Vercel 프로젝트 `climath-team1` (GitHub `minsuci/climath-team` main → 자동 배포)
- Firebase: `climath-team` (Firestore 서울, Authentication은 커스텀 토큰만 — 익명 켜지 말 것)
- 환경변수: `TEAM_SERVICE_ACCOUNT` = climath-team 서비스 계정 JSON 한 줄. Production/Preview/Development 셋 다.
  선택: `CLASS_API_URL` (기본 `https://climath-class.vercel.app/api/auth`) · `GITHUB_TOKEN` (비공개 저장소를 읽거나 한도를 올릴 때)

## 구조 — Firebase 프로젝트 둘

| | 프로젝트 | 무엇 | 쓰기 |
|---|---|---|---|
| `firebase.app()` 기본 | climath-class | teachers · classes · appConfig · exams · scores | 읽기만 |
| 〃 | 〃 | **students · classes.roster** | **쓴다** — 학생 명단이 이 앱에서 관리된다 |
| `teamApp` | climath-team | `dash/tasks`(팀 할 일 공개분) · `dash/config`(시트·저장소) · `dash/students`(메모) | 여기만 |
| 〃 | 〃 | `dash/tasksLead` — **팀장 전용 할 일** | 〃 |
| 〃 | 〃 | `minutes`(회의록) — **`open` 인 것만 선생님이 읽는다** | push-minutes.mjs |
| 〃 | 〃 | **`marks/<tid>`** — 할 일의 «내가 끝냈다» 표시 | **선생님도 자기 것만** |

브라우저가 두 프로젝트에 따로 로그인한다. `/api/auth login` 이 `classToken`(수업관리 앱이 발급)과
`teamToken`(이 프로젝트 서비스 계정이 서명)을 함께 주고, 각각 `signInWithCustomToken` 한다.
**둘 다 첫 `onAuthStateChanged`를 준 뒤에만 `boot()`** — 한쪽만 복구된 순간 로그인 화면이 깜빡인다.

> [!warning] 로그인하는 동안 boot 을 막아야 한다 (`LOGGING_IN`)
> 두 프로젝트에 차례로 로그인하는데, **첫 번째가 끝나면 그 자리에서 `onAuthStateChanged` 가 돈다.**
> 그때 `boot()` 이 돌면 "한쪽만 로그인됨"으로 보고 `signOutAll()` 로 방금 된 로그인을 도로 끊는다.
> 화면은 오류 없이 로그인 창으로 되돌아가서, 무엇이 틀렸는지 알 길이 없다 (2026-09-04에 이걸로 막혔다).
> 실패 메시지도 전역 `LOGIN_ERR` 에 둔다 — 실패 직후의 signOut 이 `renderLogin` 을 한 번 더 돌려
> 인자로 넘긴 메시지를 지운다.

PIN 대조·시도 제한·선생님 명단은 수업관리 앱에만 있다. 이 앱은 그 서비스 계정 키를 갖지 않는다.
앱이 답한 역할(owner·teacher)을 teamToken 에 그대로 싣는다. 학생이나 모르는 역할은 403.

## 선생님은 읽기만 (2026-09-05)

"다른 선생님들도 쓸 수 있게" → 셋 중 **«전부, 읽기만»** 을 골랐다. 보이는 것은 관리자와 같고 아무것도 못 고친다.

- **쓰기는 한 군데서 막는다** — `guardWrites()` 가 Firestore `DocumentReference.set/update/delete`,
  `CollectionReference.add`, `WriteBatch.commit` 을 가로채 `S.ro` 면 거절한다(`RO_MSG`).
  쓰는 자리가 스물여섯 군데라 하나씩 막지 않았다. 서버로 가는 쓰기(`classAuthApi` = PIN 초기화)도 같이 막는다.
  > [!warning] 앱 DB(climath-class)는 이 가로채기가 유일한 문턱이다
  > 그쪽 규칙은 선생님 쓰기를 허용한다(앱에서 고치는 게 정상이니까). 팀 DB 는 규칙이 한 번 더 막지만
  > 학생 명단·참여표·학교 일정은 앱 DB 라 **가로채기가 빠지면 선생님이 대시보드에서 지울 수 있다.** `tools/test-ro.js` 가 지킨다.
- **고치는 단추는 감춘다** — `applyReadOnly()` 가 `.btn`, `data-ddel`/`data-sdel`, 그리고 글자가 `RO_WORDS`(저장·지우·빼기·초기화·가져오·관리…)에 걸리는 단추를 숨기고,
  체크박스·textarea 는 잠그고, 찾기 칸이 아닌 text/date 칸은 readOnly. `MutationObserver` 가 화면이 바뀔 때마다 다시 건다.
  > [!warning] 로그인 화면에는 절대 걸리면 안 된다
  > «들어가기» 가 `.btn` 이다. 로그인 실패·로그아웃으로 돌아오면 `S.ro` 가 켜진 채라
  > **단추가 통째로 사라져 다시 들어갈 수가 없었다**(2026-09-07). 두 겹으로 막았다 —
  > `renderLogin()` 이 `S.ro`·`S.claims` 를 내리고, `applyReadOnly()` 는 `.card.login` 이 있거나
  > `S.claims` 가 없으면 손대지 않는다.
  글자로 가르는 건 어설프다 — **새 단추 이름에 저 말들이 들어가면 읽기 계정에서 사라진다**는 뜻이다. 막는 건 가로채기고 이건 헷갈리지 않게 하는 것.
  «다시 읽기»·거르기 칩·찾기 칸·«앱 열기» 링크는 남는다.
- 머리띠에 `이름 · 읽기만` 표, 위에 안내 배너. 로그인 화면은 앱 PIN 그대로.
- **회의록은 «팀회의»만 보인다** (2026-09-07) — 간부회의는 목록에도 안 뜬다. 아래 «회의록» 절.
- 서버: `api/auth.js` 가 teacher 도 통과, `api/github.js` 는 읽기라 teacher 도 됨. `api/sheets.js`·`api/schedule.js` 는 owner 만(쓰기).
- `firestore.rules`: **컬렉션마다 따로** 적는다 — 읽기 owner·teacher, 쓰기 owner
  (minutes 는 `open` 인 것만, marks·myTasks 는 본인, dash/tasksLead 는 팀장만).
  **콘솔에 붙여넣어 게시해야 걸린다.** 게시한 뒤에는 반드시 —
  ```bash
  node tools/check-rules.mjs
  ```
  **선생님 계정 토큰을 만들어 진짜로 불러 본다.** 서비스 계정은 규칙을 통과해 버려 소용없고,
  «잘 되는지»만 봐서는 반쪽이다 — **막혀야 할 것이 막히는지**를 본다.
  자국은 안 남긴다(성공하는 쓰기 하나만 시험하고 없던 문서면 지운다).
  2026-09-07 게시 뒤 12건 전부 확인했다.

  > [!warning] 규칙은 프로젝트가 둘이다 — 한쪽만 확인하면 못 들어온다
  > `check-rules.mjs` 는 **팀 DB(climath-team)만** 본다. 앱 DB(climath-class)는 규칙도 열쇠도 저쪽 것이라
  > 여기서 못 부른다. 2026-09-07 선생님 로그인이 통째로 막혔는데, 막은 것은 팀 규칙이 아니라 **앱 규칙**이었다.
  > 앱 DB 쪽은 `tools/test-ro.js` 가 «선생님으로 `loadCore()` 가 끝까지 가는가» 로 지킨다 — 아래 «선생님 명단».
  `tools/publish-rules.mjs` 를 만들었지만 서비스 계정에 규칙 게시 권한이 없어 403 — IAM 에서 «Firebase Rules 관리자»를 주면 된다.

> [!warning] String.replace 의 치환 문자열에서 `$$` 는 `$` 가 된다
> 패치 스크립트로 `$$(".card button")` 을 넣었더니 `$(".card button")` 이 되어 `.forEach` 에서 터졌다.
> 치환은 함수(`s.replace(a, () => b)`)나 `split/join` 으로. 되돌리려다 부분 일치로 `$$$(` 도 만들었다.

### 시험

```bash
node tools/test-ro.js
```

73건. 프로토타입이 있는 가짜 Firestore 로 set/update/delete/add/commit 이 전부 거절되는지, 거절된 것이 SDK 까지 안 가는지,
단추·칸이 맞게 감춰지는지(«다시 읽기»·칩·찾기·«앱 열기»·«내 완료» 는 남는지),
회의록을 선생님일 때만 `where("open","==",true)` 로 받아오는지,
**선생님으로 `loadCore()` 가 끝까지 가는지**(= 로그인이 되는지), 로그인 화면에서 «들어가기»가 안 감춰지는지,
**참여표에 낸 문**(`saveExamRo`)이 담당 반의 `math`·`back` 두 칸만 지나가게 하고 쓰고 나서 닫히는지,
그리고 **규칙 파일**에 «전부 열기» 줄이 없고 minutes 의 teacher 읽기에 `open` 조건이 붙어 있는지.

가짜 Firestore 는 **앱 규칙도 흉내낸다** — 선생님이 `teachers` 목록을 부르면 진짜처럼 거절한다.
흉내가 진짜보다 너그러우면 «로그인이 안 된다» 를 여기서 못 잡는다(실제로 못 잡았다).

## 선생님 명단 — 앱 DB 에서 못 읽는다 (2026-09-07)

선생님이 로그인하면 **«Missing or insufficient permissions»** 만 뜨고 못 들어왔다. 팀장은 됐다.

> [!warning] 앱의 `teachers` 문서에는 PIN 이 들어 있다
> 그래서 앱 규칙은 `allow read: if isOwner() || tid == 자기 tid` — **문서 이름**을 보는 조건이다.
> 목록 쿼리는 그걸 증명할 수 없어 Firestore 가 **통째로 거절**한다. `loadCore()` 의 맨 앞줄이 그 목록이라
> boot 이 거기서 터졌다. **규칙이 맞다. 부르는 쪽을 고쳤다** — 규칙을 열면 남의 PIN 이 새어 나간다.

- 팀장은 원래대로 앱 DB 에서 읽고, **PIN 을 뺀 사본**(`TEACHER_KEEP`)을 팀 DB `dash/teachers` 에 남긴다.
  바뀐 게 없으면 안 쓴다. `dash` 는 규칙이 이미 팀 전체에 열려 있어 **규칙을 안 고쳤다.**
- 선생님은 그 사본을 읽는다. 사본이 아직 없으면 `/api/auth` 의 목록(로그인 화면이 쓰는 것)으로 버틴다 —
  담당 반이 없어 «담당» 칸이 비지만 못 들어오는 것보다 낫다. 팀장이 한 번 열면 채워진다.
- 앱 DB 읽기는 이제 `coreRead()` 로 감싼다. 하나가 막혀도 로그인은 되고 **무엇이 막혔는지 배너에 이름이 뜬다.**
  전에는 영어 한 줄만 남아 어디서 막혔는지 알 수가 없었다.
- 참여표는 **읽기는 전원, 쓰기는 담당 반만** 이다(앱 규칙 `exams`: `read: isTeacher()` · `write: teaches(cid)`).
  팀체크가 반을 가로질러 보여주는 화면이라 읽기를 열었다(2026-09-07). 쓰기 쪽은 «선생님이 넣는 두 칸»을 볼 것.

## 팀 할 일 — 내 것과 자기 체크 (2026-09-06)

혼자 쓰던 것을 팀으로 돌리는 첫 걸음. 없던 것은 **끝냈다고 표시할 자리**였다 —
회의록에서 자기 임무를 찾는 것까지는 됐는데 그 다음이 없었다.

**둘로 나눈다. 하나로 합치면 «선생님은 완료라는데 팀장은 아니라고 본다» 를 가릴 데가 없다.**

| | 무엇 | 누가 쓰나 |
|---|---|---|
| `dash/tasks` 의 `status` | 열림·진행·완료 — 목록의 공식 상태 | 팀장만 |
| `marks/<tid>.done[할일id]` | «내가 끝냈다» + 표시한 날 | 그 사람만 |

- 화면: 내 줄에 **내 완료** 단추, 여럿이 걸린 줄에 `2/3`(누가 했고 누가 남았는지는 툴팁).
  거르기 칩에 «@내 것», 통계에 «내 것». 선생님이 열면 `S.filterWho` 가 «@내 것» 으로 시작한다.

### 담당 칩 (2026-09-08 고침)

칩에 세우는 사람은 `WHO_CHIPS` 에 적힌 **다섯 명**이다 — 이창혁A · 이승엽 · 박준성 · 정찬준 · 이현우.

> [!warning] 담당 글자를 전부 칩으로 세우면 거르는 칸이 거르는 일을 못 한다
> 예전에는 할 일에 적힌 담당 글자를 모아 칩을 만들었다. 회의록에서 굴러 들어온
> «전원 (팀 확인은 한민수)» · «고등부 (한민수 진행)» · «해당 반 담당» · «희망자» · «데스크» 까지
> 칩이 되어 **스물넷이 넘고 넉 줄**이 됐다. 그러면 아무도 안 누른다.

- 칩에 없는 이름도 **담당으로 쓸 수는 있다.** 그런 할 일은 «전체» 와 그 사람의 «@내 것» 에서 보인다.
- 담당을 **적을 때** 뜨는 추천(`whoOptions()`)은 안 좁혔다. 칩은 보는 것이고 추천은 적는 것이라
  «전원» · «담임 전원» · 한민수처럼 칩에 없는 것도 그대로 골라야 한다.
- 칩에 그 사람 몫이 몇 건인지 붙는다. 눌러 보기 전에 안다.

> [!warning] 칩은 글자로 찾으면 안 된다
> `taskMatchesWho` 가 `indexOf` 였다. 그러면 «전원» · «고등부» · «담임 전원» 으로 걸린 일이
> **아무의 칩에도 안 나온다** — 그 사람 일인데도 담당 칸에 이름이 없어서다.
> 마왕님 말씀 — «전원은 그냥 각자 모두에게 들어가는 것으로». `taskIsFor()` 를 쓴다(«@내 것» 과 같은 셈).
- 담당 글자가 누구인지는 회의록 칠하기와 **같은 `personGroups()`** 로 본다 — 두 군데가 다르게 판단하면 안 된다.
  `whoHasTerm()` 이 담당 칸을 `·` 로 쪼개 **조각 앞에서부터** 맞춘다.
  > [!warning] 그냥 `indexOf` 로 찾으면 남의 일을 떠안는다
  > «담임 전원» 안에 «전원» 이 있어 반도 없는 사람이 담임 일을 받고,
  > «예비고1 담당 강사» 안에 «고1 담당» 이 있어 고1 선생님이 예비고1 일을 받는다.
  > 뒤도 봐야 한다 — «이창혁A» 를 «이창혁» 으로 읽으면 안 된다.
- 읽기 계정의 쓰기 가로채기(`guardWrites`)에 **문 하나**를 냈다: `passWrite()` 가 `RO_PASS` 를 켜는 동안만 지나간다.
  `toggleMyMark()` 와 `saveExamRo()` 둘뿐이고 부르자마자 끈다. **여기를 넓히면 «전부 읽기만» 이 무너진다.**
  문을 낼 때마다 «누가·무엇을» 을 함수 안에서 다시 확인한다 — `passWrite` 자체는 아무것도 안 가린다.
  단추에는 `data-keep` — 없으면 `applyReadOnly` 가 «완료»·«✓» 글자를 보고 감춘다.
- 저장에 실패하면 화면도 되돌린다. 안 그러면 «했다고 눌렀는데 팀장에게는 안 보인다» 가 된다.
- `firestore.rules` 에 `match /marks/{tid}` — 선생님은 `tid == request.auth.token.tid` 인 자기 문서만 쓴다.
  **콘솔에 붙여넣어 게시해야 선생님의 «내 완료» 가 저장된다.** 그 전에는 거절되고 표시가 되돌아간다(관리자는 그냥 된다).

### 각자 적는 할 일 — `myTasks/<tid>` (2026-09-07)

선생님도 **자기 할 일은 직접 적는다.** 남의 할 일을 만드는 것은 팀장뿐이다.

> [!warning] 공용 목록에 선생님 쓰기를 열면 «남에게 시키기» 가 같이 열린다
> `dash/tasks` 는 한 문서 안의 배열이라 규칙이 담당 칸을 못 본다 — 아무 이름이나 적을 수 있다.
> 그래서 `marks` 와 같은 모양으로 **사람마다 문서 하나**를 준다. 문서 이름이 곧 누구인지다.

- 규칙: `match /myTasks/{tid}` — 읽기는 팀 전체(팀장이 봐야 한다), 쓰기는 `owner` 이거나 `tid == myTid()`.
  **콘솔에 게시해야 «내 할 일 추가» 가 저장된다.** 그 전에는 «적지 못했다» 가 뜨고 화면에서도 지워진다
- **담당은 문서 주인에서 다시 만든다** (`ownTasks()`). 규칙은 배열 안을 못 보니,
  자기 칸에 남의 이름을 적어 둬도 화면에는 자기 이름으로 나온다
- 화면 표식은 `own: <tid>`. 저장할 때 뗀다(`stripOwn`). `saveTaskOf(t)` 가 줄을 보고 **어디에 저장할지** 고른다 —
  공용 목록이면 팀장 몫, 자기 것이면 자기 문서. `saveTasks()` 는 `own` 이 붙은 줄을 **빼고** 쓴다(안 빼면 둘로 늘어난다)
- `canEditTask(t)` = 팀장이거나 자기가 적은 줄. 아니면 체크칸·상태·지우기를 **아예 안 건다** —
  걸어 두면 눌렀을 때 조용히 거절만 당하고 화면은 그대로다
- 선생님 화면의 추가 칸에는 **담당이 없다**(자기 자신이다). 칸·단추·체크칸에 `data-keep` 이 붙는다 —
  없으면 `applyReadOnly` 가 잠가서 «적어 놓고 못 끄는 목록» 이 된다
- 자기가 적은 줄에는 «내 완료» 를 안 붙인다. 체크칸으로 바로 끝낸다 — 표시할 상대가 없다

### 강사가 봐도 되는 할 일인가 (2026-09-07)

회의록을 갈랐으면 **거기서 뽑은 할 일도 갈라야 한다.** 회의록만 닫고 할 일을 열어 두면 닫은 의미가 없다.

**기준은 출처가 아니라 담당이다.** 간부회의에서 나와도 담당이 «전원» 이면 강사가 할 일이고,
전체회의에서 나와도 담당이 팀장 혼자면 강사가 볼 것이 아니다.
`taskShared(t)` — **담당에 팀장 말고 우리 팀 사람이 하나라도 걸리면 공개.**

| | 어디에 | 누가 보나 |
|---|---|---|
| 담당에 강사가 걸린 것 (전원·담임 전원·고등부·강사 이름) | `dash/tasks` | 팀 전체 |
| 팀장 혼자 · 담당 없음 · 팀 밖 사람(고우빈·김효상·데스크) | `dash/tasksLead` | **팀장만** |

> [!warning] 한 문서 안의 배열은 규칙이 못 가른다
> 그래서 **문서를 둘로 나눴다.** 안 나누고 화면에서만 거르면 선생님이 콘솔에서 통째로 읽는다 —
> 회의록에서 겪은 것과 똑같은 자리다.

- 선생님은 `tasksLead` 읽기가 규칙에 막혀 조용히 빈 배열이 된다. 그러면
  **할 일·업무 달력·앞으로 2주가 저절로 공개분만 그린다** — 화면마다 거를 필요가 없다.
- 팀장 화면에는 전용 할 일에 `🔒` 가 붙는다. 무엇이 닫혀 있는지 보여야 실수를 안다.
- `saveTasks()` 가 저장할 때마다 다시 가른다. 담당을 고치면 그 자리에서 옮겨 간다.
- 규칙은 `match /dash/{doc}` 에 `doc != "tasksLead"`. **`{doc=**}` 로 되돌리면 doc 이 Path 가 되어
  이름 비교가 무너진다** — `test-ro.js` 가 그것까지 본다.

### 회의록에서 할 일 끌어오기 (2026-09-07)

회의록마다 «## 할 일» 표가 있다(할 일 | 담당 | 기한). 그걸 읽어 **아직 목록에 없는 것**을 찾아
할 일 화면 위에 «회의록에서 온 할 일 N건» 으로 띄운다. 줄마다 «넣기»·«안 넣기», 또는 «전부 넣기».

> [!warning] 찾아만 주고 넣는 것은 사람이 누른다
> 회의록의 할 일에는 그대로 옮기면 안 되는 것이 섞인다 — 데스크 회의록의 «전부 데스크 몫이라
> 할 일 추적에는 안 넣는다» 가 그렇고, 볼트에서 손질해 이미 들어온 것도 글자가 조금 달라 겹친다.
> 자동으로 밀어 넣으면 목록이 쓰레기가 되고, **그러면 아무도 안 본다.**

- `mdSection(md, "할 일")` 이 그 절만 뗀다 — **다음 `## ` 에서 끊는다.**
  안 끊으면 «정해야 할 것»의 표까지 할 일로 딸려온다
- 겹치는지는 **글자**로 본다(볼트에서 들어온 것과 같은 기준). 「안 넣기」는 `dash/config.taskSkip` 에
  `회의록id|할 일` 로 적어 둔다 — **기억 안 하면 누를 때마다 도로 올라와서 결국 안내를 통째로 무시하게 된다**
- 회의록은 그 메뉴를 열 때만 받아오므로, **할 일 화면을 열 때도 받아온다**(팀장일 때만).
- 선생님은 할 일을 못 쓴다. 이 기능은 통째로 팀장 것이다 — `newFromMinutes()` 가 `S.ro` 면 빈 배열.

### 시험

```bash
node tools/test-tasks.js
```

106건. 담당 글자 맞추기(위 함정 전부), 몇 명이 걸렸나, «@내 것» 거르기, **담당 칩**(다섯 명만 서는지,
«전원»·«고등부» 가 각자에게 들어가는지, 적을 때 추천은 안 좁아졌는지),
**공개·전용으로 갈리는지와 저장이 문서 둘로 나뉘는지**(하나도 안 새고 겹치지도 않는지),
자기 문서에만 쓰는지, **체크가 끝나면 문이 다시 닫히는지**, 실패하면 되돌리는지, 줄에 그려지는 것,
그리고 **회의록에서 끌어오기** — 절을 다음 제목에서 끊는지, 이미 있는 것·«안 넣기» 한 것이 빠지는지.

> [!warning] 같은 이름의 함수를 또 만들면 나중 것이 조용히 이긴다
> `markOf()` 를 새로 만들었는데 내신 달력에 이미 있었다. 문법 검사도 화면 그리기도 통과했고
> 체크가 그냥 안 보였다. 4000행이 한 파일이라 눈으로는 못 본다 —
> `tools/test-wiring.js` 가 이제 중복 선언을 훑는다.

## 학생 명단 — 이 앱이 근거지 (2026-09-04)

> [!note] 2026-09-08부터 **고치는 곳도 여기 하나뿐**이다
> 수업관리 앱의 명단 편집을 닫았다(그쪽 `ROSTER_RO`). 그 앱에서는 보기·PIN 초기화·반 안 순서·선생님 전환만 된다.
> **자료는 여전히 앱 DB 에 있다** — 옮기지 않았다. 출결·과제·점수·학생 로그인이 반 명단의 자리 번호에 달려 있어서
> 그것을 옮기는 것은 이사가 아니라 재건축이다. 여기서 고치고 저기에 쓴다.
> 그래서 **팀체크가 못 하는 일이 생기면 명단이 어디서도 안 고쳐진다.** 사람 만들기·이름·학교·학년·담임·반 배정·
> 삭제·PIN·«사람으로 만들어 잇기» 가 다 여기 있어야 한다. 하나라도 빠지면 그쪽 깃발을 되돌려야 한다.

만들기·고치기·반 배정·지우기를 여기서 한다. **앱에서도 같은 일을 할 수 있다** —
저장되는 곳이 한 군데(앱 `students` + 반 `roster`)라 어느 쪽에서 고쳐도 같은 것이 바뀐다.

**근거지라는 말은 저장소가 여기라는 뜻이 아니다.** 어디서 무엇이 바뀌었는지를
여기서 다 본다는 뜻이다 — 명단 편집의 «최근 바뀐 것»이 그것이고,
앱에서 고친 것도 함께 뜬다 (`appConfig/studentLog`, 앱 DB).
저장소를 이 앱으로 옮기면 학생 로그인·출결·참여표가 전부 여기 매달린다. 그래서 안 옮겼다.

### 배치표는 고1 줄 · 예비고1 줄 (2026-09-08)

마왕님 말씀 — "예비고1이랑 고1이랑 한줄에 있는데 위에 고1, 아래 예비고1로 정리하자".
`classGroup` 이 **반 이름**으로 가른다(반 문서에 학년 칸이 없다) — `GRID_GROUPS` 순서로 고3·고2·고1·예비고1, 안 걸리면 «그 밖».
줄마다 머리(학년 표 · N반 · M명)와 표 하나(`classGridTable`). 반 미배정은 맨 아래 줄. 줄이 하나뿐이면 머리를 안 단다.
시험은 `tools/test-roster-sheet.js` 의 «배치표는 고1 줄 · 예비고1 줄» 묶음.

> [!warning] 사본을 여기 두지 않는다
> "여기서 만들고 앱으로 보낸다"를 **두 벌**로 만들면 주인이 둘이 된다. 다른 선생님이 앱에서 넣은
> 학생이 이쪽에 안 보이고 다음 보내기에 조용히 덮인다. 학생 로그인·출결·참여표가 전부
> climath-class 를 보므로 진실은 거기 하나여야 한다. **이 앱은 고치는 자리이고 저장은 앱 DB 한 곳.**
> 앱에 없는 칸(메모)만 `dash/students` 에 붙인다.

관리자 토큰이면 앱 규칙이 이미 `students` 쓰기와 `classes` 갱신을 허용한다 — **앱 규칙은 안 건드렸다.**

- `stSave/stCreate/stDelete` — `students/{pid}`. **지우기는 반 명단에서 먼저 빼고** 사람 문서를 지운다.
  > [!warning] 사람만 지우면 유령이 남는다
  > 앱의 `deleteStudent` 는 사람 문서만 지운다. 앱에서 만든 옛 항목은 `pid` 가 없어서 그래도
  > 명단에 그대로 남아 동작했다. 그런데 **여기서 만든 학생은 `pid` 가 있다.** 사람만 지우면
  > 지워진 사람을 가리키는 항목이 반에 남아 **배치표에는 뜨는데 명단에는 없고**,
  > `rosterDrift`(사람이 없어 건너뜀)도 `looseRosterRows`(pid 가 있어 건너뜀)도 못 잡는다.
  > 2026-09-04에 이걸로 유령이 하나 생겼다. `orphanRosterRows`/`removeOrphans` 가 뒤처리한다.
- `orphanRosterRows()` / `removeOrphans()` — 지워진 사람을 가리키는 반 항목. 편집 화면 배너에서 뺀다.
  배치표에서는 빨간 취소선으로 뜬다.
- `stSyncRosters(person)` — 이름·학교·학년을 그 사람이 든 모든 반의 `roster` 미러에 흘려보낸다.
  **이름을 빼먹으면** 출석부·참여표가 옛 이름을 계속 쓴다.
- `withFreshRoster(cid, fn)` — 반 명단은 배열 하나라 통째로 다시 쓴다. 그래서 **쓰기 직전에 반 문서를
  다시 읽고** 그 위에 얹는다. 화면이 들고 있던 사본으로 쓰면 그 사이 다른 선생님이 넣은 학생이 날아간다.
- `rosterDrift()` / `fixDrift()` — 미러가 어긋난 것을 찾아 다시 보낸다. 편집 화면 위 배너.
- `looseRosterRows()` / `linkLoose()` — 반에만 있고 사람 정보가 없는 옛 항목. **이름이 하나뿐일 때만**
  잇는다. 겹치면 건너뛴다 — 동명이인을 섞으면 되돌리기 어렵다.

### 시험

```bash
node tools/test-roster.js
```

가짜 Firestore로 쓰기 로직을 돌린다(24건). **운영 DB를 건드리는 코드라 고치면 반드시 돌린다.**
"남이 그 사이 넣은 학생이 안 지워지는가", "지우고 나서 유령이 안 남는가"까지 본다 — 눈으로는 못 잡는다.
가짜 Firestore 는 쓸 때 **반드시 직렬화(깊은 복사)해야 한다.** 참조를 그대로 두면 화면 배열과
서버 배열이 같은 것이 되어 없는 버그가 보인다(2026-09-04에 한 번 헛짚었다).

## 학교 일정 — **이 앱에서만 관리한다** (2026-09-04)

수업관리 앱에서 학교 일정 화면을 지웠다. 채우는 일은 교무팀장이 여기서 하고,
선생님들은 앱에서 참여표만 본다. 화면이 두 벌이면 어느 쪽이 맞는지 아무도 모른다.

앱에 남은 것은 읽는 쪽뿐이다(학생 화면 미리 채우기 · 일정 갈림 잡기).
서버 `api/schedule.js` 도 앱에 그대로 있고 여기서 중계해서 쓴다 — 로직은 한 벌이다.

**참여표의 «학교 일정으로 채우기» 도 여기로 옮겨왔다** (`fillTargets`·`runFill`).
채울 사람이 없으면 상자를 숨겼었는데, 그러면 "자동 채우기 단추가 없다"로만 보인다(2026-09-07). 지금은 **안 낸 사람은 있는데
못 채우는 이유**(이 회차 일정 없음 · 시작일 비어 있음 · 학교/학년 없음)를 이름과 함께 보여주고 고칠 메뉴로 보낸다(`fillTargets().why`).

> [!warning] 선생님 계정은 앱 규칙이 담당 반 참여표만 읽어 주던 때가 있었다 (2026-09-07)
> `classes/{cid}/exams` 읽기가 `inClass(cid)` 라 다른 반은 permission-denied → `.catch` 가 빈 것으로 덮어
> **다른 반 학생이 전부 «안 냈다»로 보였다.** 앱 규칙을 `isTeacher()` 로 넓혔고(앱 저장소 `firestore.rules`, 콘솔 게시 필요),
> 팀체크는 못 읽은 반을 `S.examsDenied` 에 적어 반 칩에 «못 봄», 위에 배너로 알린다. 조용히 빈 표를 그리지 않는다.

- **반 칩** — 학년 안에서 반을 골라 본다(`S.exCls`). 담임이 자기 반만 모아 보거나 팀장이 반별로 훑을 때. 반이 둘 이상일 때만 뜬다.
  학년을 바꾸면 반 선택은 풀린다. 띠·표·채우기 상자·«낸 사람» 수가 전부 고른 반 기준이다.
일정을 여기서만 채우는데 옮기는 단추가 앱에 있으면 반쪽만 옮긴 것이 된다.

## 시험 기간 찾아오기

학교·학년마다 내신 기간과 수학시험 날짜를 한 번만 적어두는 곳. 수업관리 앱의 `SchoolTermBoard` 와
**같은 문서**(`appConfig/schoolTerms`)를 쓴다. 참여표가 여기서 기본값을 가져간다.
표의 날짜 칸은 여기서 바로 고친다(`saveSchoolTerm` — **넘긴 칸만** 보낸다).

**찾아오기 서버는 2026-09-08부터 여기 있다**(`api/schedule.js`, 961줄). 나이스 → 학교 홈페이지 →
게시판 첨부문서(→ AI로 날짜 뽑기) → 메뉴에 붙은 그림, 네 단계를 **수업관리 앱에서 통째로 옮겨왔다.**
학교마다 시험을 부르는 말이 다른 규칙 뭉치가 몇 달치라 한 줄도 안 고치고 그대로 가져왔다.

> [!warning] 이 프로젝트에 환경변수 둘이 있어야 한다
> `NEIS_API_KEY` · `GEMINI_API_KEY`. **없어도 죽지는 않고 조용히 나빠진다** —
> 나이스 키가 없으면 한 번에 5건만 와서 날짜 창을 반씩 잘라 받고(느리다), AI 키가 없으면
> 첨부문서·그림에서 날짜 뽑기가 통째로 꺼진다(«AI 키 없음» 으로 적힌다). 값은 climath-class 프로젝트에 있는 것과 같다.

- **검사가 한 겹이 됐다.** 팀 토큰이 owner 인지만 본다. 브라우저도 수업관리 앱 토큰을 더 안 보낸다.
- **학교 코드 캐시가 팀 DB 로 왔다** — `appConfig/neisCodes`(앱) → `dash/neisCodes`(여기).
  옮긴 첫 판에는 비어 있어 학교를 다시 찾는다. 한 번만 드는 값이다.
- 서버에 Firestore REST 도우미(`getDoc`·`patchDoc`)가 함께 왔다(`api/_google.js`). 전에는 시트만 봤다.
  **`patchDoc` 은 칸 지정(updateMask)을 쓴다** — 학교 넷을 나란히 부르므로 통째로 덮으면 옆 요청이 넣은 학교가 지워진다.
- 떠난 자리(`climath-class/api/schedule.js`)에는 «옮겨갔다» 표지만 남겼다. 되돌릴 때를 위한 사본이라
  **한 회차 돌려 보고 지운다.** 그 사본을 고쳐도 아무 일도 안 일어난다.
- 시험: `tools/test-schedule-move.js`(옮김이 끝났나) · `tools/test-examplan.mjs` · `tools/test-schedimg.mjs`(함께 옮겨온 것)
- 학교 하나가 문서까지 가면 20~30초. 중계 함수에 `maxDuration = 60` 을 준다.
- **넷씩 나란히** 부른다(`PULL_CONC`). 한 줄로 세우면 느린 학교가 곧 전체 시간이 된다.
- 작업 상태는 **모듈(`PULL_JOB`)에 둔다.** 화면에 매어두면 다른 메뉴를 누르는 순간 통째로 날아간다.

> [!warning] 이미 적어둔 값을 덮지 않는다
> 학교가 날짜를 바꿨을 수도, 사람이 맞게 고쳤을 수도 있다. 다르면 **알려주고 고르게** 한다.
> 날짜가 같은데 이름이 옛 나이스 이름이면 **이름만** 고친다 — 이미 셋(중간고사·기말고사·졸업고사)
> 중 하나면 손대지 않는다(사람이 적은 것을 덮지 않으려고).
> 시험을 못 찾아도 **학기 계획과 근거 링크는 적어둔다** — "이번 회차에 시험이 없다"와
> "못 찾았다"를 가르는 유일한 단서이고, 손으로 채울 때 바로 필요하다.

**수학 시험 날짜는 학사일정에 없다.** 과목별 시간표는 안 올라온다.

### 수학시험 날짜 — 홈페이지에서 찾아오기 (2026-09-08)

«수학시험 날짜 넣기» 상자의 **«홈페이지에서 찾아오기»**. 서버가 학교 홈페이지의 가정통신문·공지 게시판을 뒤져
시험 시간표 글을 찾고, 첨부 문서에서 «학년마다 수학 보는 날» 을 읽는다(`want:"math"`).

**열 학교를 실제로 훑어 보고 만들었다.** 그 표본이 규칙의 근거다.

| 학교 | 무엇이 있었나 |
|---|---|
| 단대부고 | «2학기 중간고사 시간표 안내 가정통신문» · 첨부 HWP 에 표가 통째로. **되는 모양** |
| 중대부고 | 글은 있는데 첨부 PDF 가 변환이 안 된다 → 글 링크만 준다 |
| 은광여고 | 게시판 이름이 «알림마당». 지난 학년도 글이 걸려서 학년도 검사를 넣었다 |
| 세화고 | 첨부가 **범위표**뿐이라 날짜가 없다 → `looksLikeTimetable` 이 거른다 |
| 경기고 · 숙명여고 · 진선여고 | 최근 글에 아예 없다 |
| 휘문고 | 학교 CMS 가 아니라 게시판을 못 읽는다 |

> [!warning] fileSn 은 0부터인 학교가 있다
> 옛 코드는 첨부 번호를 **1·2·3 으로 찍어** 봤다. 단대부고 시간표는 `fileSn` 이 **0** 이라 통째로 안 보였다.
> 이제 글 안의 `serverFileObj["fileSn"]` 목록을 읽는다(`fileSnsIn`). 학사일정 쪽도 같이 나아졌다.

> [!warning] 표는 **재서** 읽는다. AI 는 뒷길이다 — 되돌리지 마라
> 첨부를 글자로만 이으면 «1학년·2학년·3학년이 열이었다» 는 사실이 사라진다.
> 그 뭉갠 글자를 AI 에게 줬더니 **고1 에 미적분Ⅰ·확률과통계·기하까지 여섯 과목**을 붙여 놨다.
> 고1 은 공통수학2 하나뿐이다. 날짜는 다 진짜였다 — 틀린 것은 학년이라 `verifyMath` 가 못 잡는다.
>
> 이제 `synapPages` 가 좌표가 든 XML 을 그대로 들고 오고, `gridMathPage` 가
> «1학년/2학년/3학년» 머리 칸의 **가운데 x** 로 열을 긋는다. 칸은 열 가운데에 놓이므로
> 왼쪽 끝이 아니라 **가운데**로 재야 맞는다. 날짜 칸은 세로 가운데에 있어
> **세로로 가장 가까운 날짜**가 그 줄의 날이다.
> 머리 칸이 없을 때만 `mathFromDoc`(AI) 로 넘어간다. 답에 `by: "표"|"AI"` 를 실어 화면에 밝힌다.
> → 볼트 [[표를 글자로만 꺼내면 열이 사라진다]]

- **범위표를 시간표로 받으면 안 된다.** `looksLikeTimetable` 이 날짜·교시·수학 과목 셋을 다 요구한다.
- **AI 가 지어낸 날짜는 버린다**(`verifyMath`) — 원문에 그 «월/일» 이 있고 기간 안이어야 한다.
  다만 이건 **날짜만** 본다. 학년은 못 본다.
- **«인공지능 기초» 는 수학이 아니다.** `MATH_SUBJ` 는 띄어쓰기를 지우고 본다 — «인공지능수학» 만 받는다.
- **못 찾아도 조용히 끝내지 않는다.** 글 링크와 이유를 돌려준다. 팀장이 열어 붙여넣으면 된다.
- 찾아와도 **넣는 것은 줄마다 누른다.** 붙여넣기와 같은 화면·같은 «넣기» 를 쓴다(`mathPlanFromRows`).
- 시험 `tools/test-math-crawl.js` 37건. 흉내 자료의 좌표는 **단대부고에서 실제로 잰 값**이다.
- **배포됐다고 되는 게 아니다.** 200 이 오고 JSON 모양이 맞아도 값이 틀릴 수 있다.
  서비스 계정으로 팀장 토큰을 만들어 직접 부르고 **답을 눈으로 봐야** 안다. 위 잘못을 그렇게 찾았다.

### 수학시험 날짜 넣기 — 가정통신문을 붙여넣는다 (2026-09-08)

"요즘 슬슬 학교 홈페이지 가정통신문이나 공지사항에 올라오는 것 같더라고. 수학시험날짜 넣기 기능 추가할 수 있을까?"

학교 일정 머리의 **«수학시험 날짜 넣기»** — 학교를 고르고 그 글을 그대로 붙여넣으면 수학 보는 날을 찾아 준다.
`mathExamHits` 가 두 모양을 본다.

| 모양 | 어떻게 읽나 |
|---|---|
| 칸(표를 복사하면 탭으로 온다) | 날짜가 머리줄에 가로로 서고 과목이 그 아래 온다 → **같은 칸 번호**의 날짜를 쓴다 |
| 줄 | 한 줄에 날짜와 과목이 같이 온다. 날짜가 없으면 **6줄 안쪽 위**의 날짜 줄을 쓴다 |

- **찾기만 하고 넣는 것은 사람이 누른다.** 시간표를 잘못 읽으면 직보(수학시험 전날)까지 어긋나고 참여표가 그 날짜로 돈다.
- **한 학년에 날짜가 둘이면 안 넣는다** — «여럿» 으로만 세우고 표에 직접 적게 한다.
- **시험 기간(start~end) 밖이면 빨갛게 짚는다.** 지난 회차 시간표를 붙여넣는 실수가 제일 흔하다.
- 학년 표시(«1학년»·«고2»)가 있으면 학년마다 가르고, 아예 없으면 그 학교 모든 학년에 같은 날.
- `hasMath` 는 **«수학여행»·«대학수학능력시험» 을 뺀다** — 가정통신문에 흔하다. 그 말을 지우고도 수학이 남으면 과목으로 본다.
- 해는 이 회차 시험이 있는 해로 채운다(«10/15» 에는 해가 없다).
- 시험 `tools/test-math-date.js` 36건.

> [!note] 긁어오기는 아직 안 한다
> 홈페이지에서 자동으로 찾아오려면 학교마다 다른 게시판을 뒤져야 하고, 그 파이프라인은 **수업관리 앱**에 있다
> (`api/schedule.js` 의 `boardDocText`·`examFromDoc`). 거기에 «가정통신문·공지에서 과목 시간표» 단계를 더하고
> 팀체크가 중계하면 된다. 붙여넣기는 그 사이에도, 홈페이지 모양이 어떻든 오늘 바로 되는 길이다.

앱 쪽 파이프라인에 **네 번째 길**이 있다(2026-09-04) — 메뉴 본문에 그림으로 붙여둔 학사일정.
그림에서 읽은 학교는 `via: "image"` 로 오고 결과 줄에 `(그림)` 으로 표시된다. **사람이 한 번 봐야 한다.**
→ 볼트 `30 노하우/학사일정을 그림으로 붙여둔 학교가 있다.md`

### 시험

```bash
node tools/test-terms.js
```

20건. 저장 열쇠(앱과 같아야 한다), 학기 계획 줄, 줄 모으기, 그리고 채움/덮지 않음/이름만 고침 판단.

## 내신 참여표 — 여기서 만든다 (2026-09-04)

수업관리 앱의 `ExamBoard`/`ExamEntry` 와 **같은 자리에 같은 모양으로** 쓴다. 그래서 여기서 만든 것이
앱 참여표에 그대로 뜨고, 학생이 낸 것도 여기 그대로 보인다.

```
classes/{cid}/exams/{회차키}__{sid}    회차키 = 회차의 / 와 공백을 _ 로 바꾼 것
appConfig/examTerm                     회차는 학원 전체로 하나
```

> [!warning] 문서 열쇠가 앱과 한 글자라도 다르면 서로 다른 문서에 쓴다
> `examKey(term, sid) = String(term).replace(/[\/\s]+/g, "_") + "__" + sid`.
> 조회는 `where("term","==",회차)` 라 문서에 `term` 필드도 **원문 그대로** 들어가야 한다.
> `tools/test-exam.js` 첫 두 건이 이걸 지킨다.

- **본체는 표가 아니라 날짜 띠다.** 이 표가 답하는 질문이 "누가 언제 안 오고 언제 돌아오나"라서다
  (원본 구글시트가 그랬다). 띠 = 시험 있는 달 1일 ~ 가장 늦은 복귀일. 표식은 `직` `수` `시` `첫`.
- **대신 입력은 날짜 칸이 아니라 달력에 칠한다**(`PAINT_MODES`). 시험기간은 시작·끝 두 번 누른다.
  수학시험을 정하면 직보가 전날로 따라가되 **손으로 고쳐둔 직보는 놔둔다.**
- 반 선택 없이 **담당 반을 가로질러** 모은다. 반별로 보면 "이번 주에 누가 빠지나"를 못 본다.
- 학교는 목록에서 고른다(`KNOWN_SCHOOLS` + 학생·학교일정에 있는 이름). 자유 입력이면
  중동고/중동고등학교/중동이 섞여 **같은 학교인지 판별 자체가 안 된다.**
- 같은 학교·학년인데 `examSig`(시작·끝·수학)가 갈리면 띄우기만 한다. **자동으로 안 고친다** —
  분반마다 진짜 다를 수 있고 무엇이 맞는지는 사람이 안다.
- 대신 입력을 새로 열 때 `appConfig/schoolTerms` 에 적어둔 학교 일정으로 **빈 칸을 미리 채운다.**
- **복귀와 수학시험은 표에서 바로 넣는다 — 선생님도 자기 반은 넣는다** (2026-09-10).
  이 둘은 선생님이 학교에서 듣고 오는 값이라 팀장을 거치면 늦는다. 표의 두 칸이 곧 입력이다.
  `canEditExam(cid)` 가 연다 — 팀장은 전부, 선생님은 `S.teachers[].classIds` 에 든 반만.
  `saveExamRo()` 가 `passWrite` 로 지나가되 **`math`·`back` 두 칸만** 담는다(`EXAM_RO_FIELDS`).
  수학시험을 바꾸면 직보가 전날로 따라가는 것은 대신 입력과 같다(`prepFollow`).

  > [!warning] 화면이 여는 반과 앱 규칙이 여는 반이 같아야 한다
  > 앱 규칙은 `allow write: if teaches(cid)` — `teachers/{tid}.classIds` 를 본다.
  > `canEditExam` 도 **같은 자료**(팀 DB 의 사본)를 본다. 화면만 열면 «저장했는데 안 들어간다»,
  > 규칙만 열면 아무도 못 쓴다. 사본에 `classIds` 가 실려야 하는 이유이기도 하다(`TEACHER_KEEP`).

  > [!warning] 아직 안 낸 학생은 여기서 못 만든다
  > 복귀만 든 반쪽 문서가 생기면 띠도 등원 회차도 엉킨다. 팀장이 «학교 일정으로 채우기» 를 누른 뒤에 넣는다.
  > 선생님에게는 «넣기»·«고치기»·«×» 가 감춰져 있어 새로 만들 길이 없다.

  칸에는 `data-keep` — 없으면 `applyReadOnly` 가 날짜 칸을 통째로 잠근다.
  다른 반은 칸이 아니라 **글자**로 보인다. 눌러도 안 되는 칸을 주는 것보다 낫다.
- **정렬** — 이름·반·담임·학교·내신기간·수학·등원. 위 칩이나 열 머리를 누르면
  오름 → 내림 → 원래 순서로 돈다. **빈 칸은 방향과 상관없이 늘 맨 아래** — 안 채운 사람을
  찾으려고 정렬하는 게 아니다. 같은 값이면 이름 가나다.
  > [!warning] 띠와 표에 정렬을 따로 걸면 안 된다
  > 둘이 같은 배열을 써야 한다. 따로 걸면 **이름은 같은 줄에 있는데 날짜 칸이 다른 사람 것**이 된다.
  > `renderExams` 가 `examSortRows` 를 한 번만 부르고, 띠는 그 결과에서 낸 사람만 걸러 쓴다.
  > 등원 회차 정렬은 숫자를 세 자리로 채워 비교한다 — 글자로 세면 10이 9보다 앞에 온다.

### 등원 회차 — 수강료에 쓰는 숫자

```
등원 = 그 달 수업일 − (직보 다음날 ~ 복귀 전날) + 직보·복귀
```

> [!warning] 세 가지를 틀리기 쉽다
> 1. **안 오는 구간을 내신기간(start~end)으로 재면 안 된다.** 직보가 마지막 수업이고 그 다음 오는
>    날이 복귀라, 시험이 끝난 뒤라도 복귀 전이면 안 온다. 화목반이 9/23에 끝나고 9/29에 복귀하면
>    9/24(목)은 안 오는데 내신기간으로 재면 오는 날로 잡힌다. `outRange()` 가 한 곳에서 정한다.
> 2. **요일을 안 보는 판정을 쓰면 그 달 전체가 수업일이 되어 31회로 나온다.** `isClassDay()` 를 쓴다.
> 3. **띠 범위와 회차 세는 범위가 다르다.** 띠는 복귀일에서 끝나지만 회차는 **그 달 전체**로 센다.
>    복귀가 25일이어도 26~31일 수업일은 수강료에 들어간다.

제출에 `days`(수업 요일)를 함께 저장한다 — 나중에 반 요일이 바뀌어도 그때 센 회차가 재현된다.

### 시험

```bash
node tools/test-exam.js
```

79건. 위 함정과 정렬, 중3 내신관리 파일, 선생님이 넣는 두 칸(`canEditExam`·`prepFollow`·표)까지 시험한다.
계산을 고치면 반드시 돌린다. 쓰기가 정말 지나가는지는 `tools/test-ro.js` 가 본다(73건).

## 시험 성적 — 중간·기말·모의 (2026-09-04)

메뉴 이름이 "성적 입력"이었을 때는 학생이 낸 **일일 테스트**를 세기만 했다. 팀장이 보고 싶은 건
그게 아니라 **중간고사·기말고사·모의고사**라서 다시 짰다.

앱에 없는 자료라 **이 앱 DB(climath-team)** 에 둔다. 앱 규칙을 안 건드린다.

```
tests/{tid}                { kind: midterm|final|mock, name, grade, date, created }
testScores/{tid}__{pid}    { tid, pid, name, raw, ach, grade, std, pct, avg, note, updated }
```

- 시험 종류마다 칸이 다르다 — 중간·기말은 `원점수 · 성취도(A~E) · 등급 · 과목평균`,
  모의고사는 `원점수 · 표준점수 · 백분위 · 등급`. `TEST_KINDS[kind].cols` 하나만 보면 된다.
- 표는 **그 학년 학생 전원**이 뜬다(`testRoster`). 안 본 사람도 자리가 있어야 누가 빠졌는지 보인다.
- 붙여넣기(`parseScoreLine`)는 `이름` 다음에 그 종류의 숫자 칸 차례. **성취도(A~E)는 자리를 안 가린다**
  (글자라 숫자와 안 섞인다). `-` 는 빈 칸. 이름이 겹치면 넣지 않고 알려준다.
- 요약은 등급 분포 + **담임별·반별 평균**. 팀장이 보는 화면이라 사람별이 아니라 담당별로 묶는다.
- 정렬은 이름·반·담임·학교와 점수 칸 전부. 점수는 처음 누르면 **내림차순**(잘한 사람부터가 자연스럽다),
  이름·반은 오름차순. 빈 칸은 늘 맨 아래.

> [!warning] 값이 전부 비면 문서를 지운다
> 빈 껍데기를 남기면 "응시함"으로 세어져 **평균과 인원이 틀어진다.** `saveScore` 가 확인해 지운다.

> [!warning] 시험을 지우면 성적도 같이 지운다
> 시험만 지우면 어디에도 안 보이는 성적이 남는다 — 학생 지우기에서 유령이 생긴 것과 같은 함정이다.
> `deleteTest` 가 `testScores` 를 먼저 훑어 지운다.

일일 테스트 현황은 없애지 않고 맨 아래 접어 뒀다 — "이 반은 테스트를 안 돌리고 있다"는 신호는
따로 쓸모가 있어서다. 자료가 다르다는 것을 그 자리에 적어 뒀다.

### 근거 시트에서 가져오기

성적을 따로 적는 구글시트가 있다. 시험을 고르고 **시트에서 가져오기** 를 누르면 원점수·예상등급을 끌어온다.
시트 주소는 `dash/config.scoreSheet` 에 두고 화면에서 고친다 — **공개 저장소라 코드에 안 적는다**(볼트에 있다).

시트 생김새 — 머리글이 **두 줄**이고 시험별로 칸이 묶여 있다.

```
1줄  연번 | 이름 | 학교/학년 | 소속 | 담임 | 3월 모의고사 ←─묶임─→ | 2026-1학기 중간고사 ←→ | …
2줄                                       수학(원점수) | 예상등급 | 비고 | 수학(예상등급) | 비고
```

> [!warning] 묶인 칸은 맨 왼쪽에만 값이 온다
> 구글 시트 API 는 병합 셀의 값을 **맨 왼쪽 칸에만** 주고 나머지는 빈 칸으로 준다.
> 드라이브 텍스트 내보내기는 `[merged]` 로 반복해 보여주므로 **그걸 보고 짜면 칸이 밀린다** —
> 점수가 다른 시험 칸에서 딸려온다. `parseScoreSheet` 가 1줄을 오른쪽으로 끌어 채운 뒤(ffill)
> 2줄에 이름표가 있는 칸만 점수 칸으로 본다.

- 탭마다 생김새가 다르다(학교/학년이 한 칸이기도 두 칸이기도, 기말에 과목 칸이 붙기도) — 그래서
  **자리를 못 박지 않고 글자로 찾는다**(`/^이름$/`, `/학교/`, `/원점수/`, `/등급/`).
- 탭 전체를 훑어 (탭 × 시험 묶음)을 모으고, 시험 이름으로 고른다(`matchGroup`, 띄어쓰기 무시).
  정확히 안 맞으면 비슷한 것을 고르되 **그렇다고 화면에 적는다.** 목록에서 직접 바꿀 수 있다.
- 학생은 **이름으로** 잇고, 이름이 겹치면 시트의 학교로 가른다. 못 가르면 넣지 않고 이유를 보여준다.
- 미리보기를 먼저 보여준다. **이미 넣은 값은 기본으로 안 덮는다**(체크로 덮어쓰기).
- 시트는 **읽기만** 한다.

### 시험

```bash
node tools/test-scores.js
node tools/test-sheet.js
```

각각 26건. 붙여넣기 해석·평균·등급 색·빈 값 지우기·시험과 성적 함께 지우기,
그리고 **실제 시트의 머리글 모양 그대로** 놓고 묶임 풀기·칸 찾기·학생 잇기.

## 근거 자료 (2026-09-05)

**각 메뉴가 어디서 자료를 끌어오는지**를 한 곳에서 관리한다. 여기서 바꾸면 그 메뉴가 바로 그걸 쓴다.

### 설명은 맨 아래 물음표 안에 (2026-09-08)

"자질구레한 설명들 다 빼고 맨 아래 물음표 아이콘 만들어서 거기에 넣어".
줄마다 붙던 `SOURCES[].how` 와 머리말의 서비스 계정 안내를 `srcHelpHtml()` 로 모았다(`S.srcHelp` 로 접었다 폈다).
화면에 남는 것은 **메뉴 이름 · 무엇을 대나 · 연결됨 · 단추**뿐이다.

- **글은 안 지웠다.** 처음 보는 사람에게는 다 필요한 말이라 접어 두기만 한다. `SOURCES` 의 `how` 는 그대로 쓴다 —
  줄을 새로 더할 때도 `how` 를 적으면 물음표 안에 저절로 들어간다.
- 물음표 단추에 `data-keep`. 지금은 `RO_WORDS` 에 안 걸리지만 늘면 조용히 사라진다.
- 시험은 `tools/test-sources.js` 의 «설명은 물음표 안에» 묶음 (9건) — 줄에 설명이 없고, 펼치면 전부 있는지 센다.

> [!warning] 시트를 표로 떠서 보여주지 않는다
> 처음엔 등록한 시트를 표로 그려 줬는데 **그건 시트를 그냥 여는 게 낫다.**
> 필요한 건 "이 메뉴가 무엇을 근거로 삼는가"이지 시트 내용이 아니다.
> 화면에는 근거의 이름·연결 상태·«읽히나 확인» 만 둔다.

```
dash/config.sources[열쇠]   { id, url, title, tabs, updated, checked }
```

- `SOURCES` 가 메뉴별로 한 줄씩 적어 둔 표다. `sheet: true` 인 것만 시트를 등록한다.
  나머지(학교 일정·학생 명단·회의록·할 일)는 **어디서 오는지 글로만** 적는다 — 그림이 비면
  "이 자료는 어디서 오지"를 매번 다시 뒤지게 된다.
- 옛 자리 `config.scoreSheet` 도 `sourceOf()` 가 읽어준다. 한 번 저장하면 새 자리로 옮겨간다.
  **없애면 이미 등록해 둔 시트가 사라진 것처럼 보인다.**
- 시험 성적 화면은 `scoreSheet()` = `sourceOf("scores")` 로 **읽기만** 한다.
  거기서 «바꾸기»를 누르면 근거 자료 메뉴로 보낸다 — 같은 것을 두 자리에서 고치게 하지 않는다.
- «읽히나 확인» 은 서비스 계정으로 실제로 열어 본다. 못 열면 무엇을 해야 하는지가 그대로 나온다.

### 시험

```bash
node tools/test-sources.js
node tools/test-wiring.js
```

18건(test-sources). 주소에서 id 뽑기, 메뉴별 근거 표, 옛 자리 읽어주기, 저장·끊기, 시험 성적이 그걸 그대로 쓰는지.

> [!warning] 함수를 지우면 부르는 데가 남는다 — 문법 검사는 못 잡는다
> 이 화면을 갈아엎으며 `loadSheetsAll` 을 지웠는데 `boot()` 이 계속 부르고 있었다.
> `vm.Script` 는 통과했고 **로그인이 통째로 막혔다**("loadSheetsAll is not defined").
> `tools/test-wiring.js` 가 로그인 길의 함수가 다 있는지 보고, **화면 그리는 함수를 실제로 불러본다**
> (빈 상태로 한 번, 자료를 넣고 한 번). 화면을 고치면 이걸 돌린다.
>
> 처음엔 소스를 훑어 «부르는데 없는 이름»을 찾으려 했는데, 정규식 리터럴 속 따옴표(`/[&<>"']/`)에
> 걸려 코드를 삼켰다. **파서를 흉내내는 것보다 실제로 불러보는 것이 정확하다.**

> [!warning] 비동기 시험은 맨 마지막에 건다
> `await` 로 양보하는 사이에 **뒤의 동기 줄이 `S.config` 를 갈아치운다.**
> 2026-09-05에 이걸로 없는 버그를 쫓았다.

## 앞으로 2주 — 내신 시작에 학생 이름 (2026-09-05)

이 화면의 물음은 **"누가 언제 안 오나"** 다. 학교 이름만 뜨면 매번 학생 명단을 다시 뒤지게 된다.
그래서 «내신 시작» 줄에 그 학교·학년 학생을 이름·반과 함께 붙인다(`studentsAt`).

- **시작에만 붙인다.** 수학시험·복귀에도 붙이면 같은 이름이 세 번 나와 줄이 뒤덮인다.
- **반이 없는 학생도 뺀다면 틀린다** — 그 학생도 시험 때 안 온다. 「반 없음」으로 표시만 한다.
- 명단에 그 학교·학년 학생이 하나도 없으면 그렇다고 적는다. 빈칸이면 «안 뜬 건가»로 읽힌다.

## 회의록 — 팀회의는 다 보고, 간부회의는 팀장만 (2026-09-07)

처음엔 **메뉴를 통째로** 감췄다(9/7 오전). 그런데 팀회의는 선생님들이 봐야 한다 —
그날 오후에 **회의록 하나하나로** 갈랐다.

| 볼트 머리말 | 올라가는 값 | 누가 보나 |
|---|---|---|
| `공개: 팀` | `open: true` | 선생님도 본다 |
| `공개: 간부` · 빈칸 · 칸 자체가 없음 | `open: false` | **팀장만** |

**안 적으면 닫힌다.** 간부회의가 실수로 열리는 것보다 팀회의가 실수로 닫히는 편이 낫다 —
닫힌 것은 "왜 안 보여요"로 금방 드러나지만 잘못 열린 것은 아무도 말해 주지 않는다.

- `push-minutes.mjs` 가 `공개` 를 읽어 `open` 으로 올린다. **지문(hash)에 공개도 넣는다** —
  본문만 세면 공개 칸만 고쳤을 때 안 올라간다. `--dry` 가 «팀 공개 / 팀장만»을 찍어 준다.
- 화면: 팀장에게는 간부회의에 `🔒` 와 «팀장만» 표가 붙는다. 선생님 목록에는 아예 안 뜬다.

> [!warning] 선생님 쪽 목록 쿼리에는 반드시 `where("open","==",true)` 가 붙어야 한다
> 규칙이 `resource.data.open` 을 근거로 삼는 순간, **조건 없는 목록 쿼리는 통째로 거절된다** —
> Firestore 가 "이 쿼리가 안전하다"를 증명할 수 없기 때문이다. 꾸밈이 아니라 **통과 조건**이다.
> (→ 볼트 「Firestore 보안 규칙 함정」 2번)

```
match /minutes/{doc} {
  allow read: if role() == "owner";
  allow read: if role() == "teacher" && resource.data.open == true;
  allow write: if role() == "owner";
}
```

`tools/test-ro.js` 가 규칙 파일을 읽어 **«조건 없는 teacher 읽기가 한 줄도 없는지»** 를 지키고,
가짜 Firestore 로 `loadMinutes()` 가 선생님일 때만 `where` 를 다는지 본다.
`tools/test-minutes.mjs` 가 «안 적으면 닫힘»과 지문 계산을 지킨다.

**볼트가 원본이다.** `민수의 뇌/40 팀장업무/회의록/*.md` 를 `tools/push-minutes.mjs` 가 올리고,
대시보드는 **읽기만** 한다. 고칠 곳이 둘이면 어느 쪽이 맞는지 알 수 없다 — 학생 명단과 같은 원칙이다.

```
minutes/{파일이름}   { id, date, title, kind, attend, decisions, md, hash, updated }
```

```bash
node tools/push-minutes.mjs         # 바뀐 것만 (본문 지문을 대조한다)
node tools/push-minutes.mjs --all   # 전부 다시
node tools/push-minutes.mjs --dry   # 무엇이 올라갈지 보기만 (열쇠 없이도 돈다)
```

> [!warning] 열쇠는 저장소에 안 들어간다
> 브라우저 로그인이 없는 자리라 **서비스 계정 JSON 파일**로 연다. 기본 자리는
> `C:\Users\user\.climath\team-sa.json` 이고 `TEAM_SA_FILE` 로 바꾼다.
> 스크립트는 열쇠를 화면에도 오류 메시지에도 안 찍는다. `.gitignore` 에 `*sa.json` 을 넣어 뒀다.

- **마크다운은 손으로 그린다**(`mdToHtml`). CDN 을 하나 더 늘리면 그게 죽을 때 앱이 같이 죽는다.
  볼트 노트에 실제로 쓰는 것만 다룬다 — 제목·굵게·목록·표·인용(콜아웃)·코드·취소선·링크.
  `[[볼트 링크]]` 는 갈 곳이 없으니 글자만 남긴다.
- **글자를 먼저 이스케이프하고** 마크다운을 입힌다. 날 태그가 화면 코드가 되면 안 된다.
- 본문이 길어서 **그 메뉴를 처음 열 때만** 받아온다(`showPage` 에서 lazy load).

### 참석자 이름으로 훑기 (2026-09-05)

참석 줄의 이름을 누르면 본문에서 그 사람 것만 눈에 들어온다.

| 색 | 뜻 | 규칙 |
|---|---|---|
| 초록 `mark.hit` | 이름이 나온 곳 | 그 밖의 모든 자리 |
| 붉은색 `mark.task` | **그 사람에게 맡겨진 일** | ① 표 머리에 `담당·담당자·누구` 칸이 있고 그 칸에 이름이 있을 때<br>② 바로 위 제목이 `할 일`·`지시` 인 표·목록 안에 있을 때 |

- **이름만이 아니라 덩어리째 칠한다**(`hiClass`). 이름 한 낱말만 칠하면 «그 줄이 무슨 이야기인지»가
  안 보인다. 문단·목록 항목·표 줄·인용 상자에 `hl-hit`/`hl-task` 를 붙인다.
  **한 덩어리 안에 붉은 것이 하나라도 있으면 그 덩어리는 붉다** — 담당 칸에만 이름이 있어도 그 줄 전체가 그 사람 일이다.
- **«정해야 할 것» 은 초록이다.** 거긴 담당이 **아직 안 정해진** 것들이라, 붉게 칠하면 «맡았다»로 읽힌다.
- 담당 칸이 없는 표(예: `아이템 | 내용`)의 이름은 초록이다. 글로 쓴 배정과 표에 적힌 배정을 가르는 선이
  거기다 — 표가 곧 할 일 추적으로 옮겨가는 원본이다.
### 이름 말고 소속으로도 (2026-09-05)

회의록은 사람을 **이름으로만 부르지 않는다.** «예비고1 담당 강사»·«담임 전원»·«고등부»·«전원» 도 그 사람이다.
이름만 찾으면 **정작 자기가 맡은 일을 놓친다** — 9/3 회의 유튜브 항목이 그랬다.

`personGroups(name, note)` 가 그 사람을 가리키는 말들을 모은다. **앱이 아는 것으로만** 판단한다.

| 말 | 언제 그 사람인가 |
|---|---|
| `전원` | 늘 |
| `고등부` | 우리 팀 선생님이면 |
| `담임` · `담임 전원` | 맡은 반이 있으면 |
| `예비고1 담당 강사` 같은 것 | **맡은 반 이름에서 학년을 읽는다** (`예비고1S 월금` → `예비고1`) |
| `간부` | 그 회의 참석 줄에 있고 회의 종류가 간부면 |

> [!warning] 맨 학년 이름(`예비고1`)은 소속으로 안 쓴다
> "예비고1 D등급 명단" 은 학생 이야기지 선생님 일이 아니다. **사람을 가리키는 말**
> (`담당 강사`·`담당`·`강사`·`선생님`·`담임`)이 붙었을 때만 그 사람으로 본다.

> [!warning] 앞 글자를 봐야 한다
> `예비고1 담당 강사` 안에는 `고1 담당 강사` 가 들어 있다. 그냥 찾으면 **고1 선생님까지 걸린다.**
> `findTerm()` 이 앞 글자가 한글·숫자·영문이면 건너뛴다. 긴 말부터 찾는 것도 같은 이유다.

> [!warning] 덩어리 판정은 닫는 따옴표까지 보면 안 된다
> 소속으로 걸린 것은 `class="task g"` 다. `'<mark class="task"'` 로 찾으면 **줄이 안 칠해진다.**
> 2026-09-05에 이걸로 표 줄이 통째로 안 칠해졌다.

- 참석 줄은 `attendNames()` 가 가른다. `대치 — 원장 · 고우빈 팀장` 처럼 **지역이 앞에 붙으므로 떼야 한다**.
  이름은 첫 낱말(`고우빈 팀장` → `고우빈`)이다 — 본문은 직함을 붙이기도 떼기도 한다.

> [!warning] 태그 속을 칠하면 화면이 깨진다
> `markName()` 은 `<...>` 를 건너뛰고 글자 부분만 바꾼다. 통째로 바꾸면 `href` 안의 이름까지 칠해진다.

### 시험

```bash
node tools/test-minutes.mjs
```

78건. 머리말 읽기, 마크다운 그리기, **이름 훑기**(초록·붉은색 가르기·덩어리째), 그리고
**진짜 회의록 한 편을 통째로** 그려 태그가 안 새는지 본다.

## 학년 업무 달력 (2026-09-04)

원장님이 8/31 간부회의에서 지시한 그것 — *"이번 주에 요일별로 뭐 해야 되고 뭐 보고해야 돼"*.
그래서 **달력이 본체가 아니라 「이번 주까지 해야 할 일」이 본체**다. 달력은 그걸 요일에 늘어놓은 것이라
목록을 위에, 달력을 아래에 둔다.

> [!warning] 할 일을 두 벌로 나누지 않는다
> 팀 할 일과 **같은 곳**(`dash/tasks`)을 쓴다. 나누면 어느 쪽이 맞는지 알 수 없다 —
> 학생 명단에서 배운 것과 같다. 여기서 더 쓰는 칸은 `grade`(학년)와 `repeat`(반복)뿐이다.

```
한 번 하는 일 : { due, status }
반복하는 일   : { repeat: { dow: [1,5] }, doneOn: { "2026-09-05": true } }
```

> [!warning] 반복은 날짜마다 따로 끝난다
> `status` 하나로 다루면 **이번 주에 했다고 다음 주도 끝난 게 된다.** `doneOn` 에 날짜를 적는다.
> `taskDoneOn()` 이 한 곳에서 가른다 — 반복이면 `status` 를 아예 안 본다.

> [!warning] 주는 월요일에 시작한다 — 일요일이 함정
> `getDay()` 가 일요일에 0 이라 그냥 빼면 **다음 주 월요일**이 나온다. `weekStart()` 가 0 을 따로 본다.

화면은 **월간 달력 위, 주간 업무 달력 아래**다. 둘은 **날짜 하나**(`S.calAnchor`)를 같이 본다 —
따로 두면 위아래가 다른 때를 가리킨다. 월간의 날을 누르면 그 주가 아래에 열린다.

> [!warning] 월간 격자는 주 단위로 꽉 채운다
> 그 달만 잘라 넣으면 **월말에 걸친 주가 통째로 안 보인다.** 앞뒤로 지난달·다음달 며칠이 딸려 온다.
> 달을 옮길 때는 날짜가 넘치지 않게 붙든다(`shiftAnchorMonth`) — 1/31 에서 한 달 뒤는 2/28 이다.

- 학년 탭은 `전체 / 중3 / 고1 / 고2 / 고3 / 공통`. **학년이 없는 할 일은 「공통」**이고, 특정 학년을
  고르면 안 나온다(전체에서는 나온다). 안 그러면 학년 달력이 공통 업무로 뒤덮인다.
- 손으로 안 넣어도 뜨는 것(`calRanges`) — **시험 성적에 만든 시험**과 **반 종강**뿐이다.
  일정이지 할 일이 아니라 체크가 안 붙는다.

> [!warning] 학교 내신 일정은 업무 달력에 안 넣는다 (2026-09-05 결정)
> 학교가 서른 곳이면 시험철에 달력이 시험 막대로 뒤덮여 **정작 할 일이 안 보인다.**
> 그건 «내신 참여표»의 날짜 띠가 훨씬 잘 보여준다. 넣고 싶어지면 그 이유를 먼저 적을 것.

> [!warning] 막대 높이를 못 박지 말 것
> `height: 15px` 로 두었더니 아이폰에서 **글자가 위아래로 잘렸다.** iOS 사파리가 작은 글씨를
> 저 혼자 키우기 때문이다(text inflation). `html { -webkit-text-size-adjust: 100% }` 로 막고,
> 막대는 `min-height` + `padding` 으로 자라게 둔다.

> [!warning] 시험은 기간이지 하루가 아니다
> 날마다 칩을 하나씩 찍으면 사흘짜리 시험이 **끊긴 조각 셋**으로 보인다 — 언제 시작해 언제 끝나는지가
> 눈에 안 들어온다. 시작~끝을 쥔 채 다니다가 그릴 때 `weekSegments()` 가 주 단위로 잘라 눕힌다.
> 주 경계에서 잘린 쪽은 모서리를 안 둥글리고 `◂` 를 달아 **이어진다는 것**을 보인다.
> 겹치는 막대는 층(lane)을 달리하고, 안 겹치면 빈 아래층으로 내려온다.

> [!warning] 겹치는 것은 **한 막대로 묶는다** (`mergeRanges`)
> 학교가 스무 곳이면 시험철에 층이 스무 줄로 늘어난다 — 달력이 아니라 줄무늬가 된다.
> 겹치는 것끼리 하나로 묶고 앞에 개수를 단다(`가고 외 4`). 손을 올리면 전부 보인다(`title`).
> **같은 종류끼리만** 묶는다 — 내신 기간과 수학시험을 한 막대에 넣으면 무슨 날인지 알 수 없다.
> 맞닿기만 한 것(3일 끝 · 4일 시작)은 안 묶는다. 다른 시험 창이다.
> A와 C가 안 겹쳐도 B가 둘을 이으면 셋이 한 막대가 된다(사슬).
> 날 칸(`.mday`)은 배경으로 깔고 막대는 그 위에 눕는다 — 칸 안에 넣으면 칸을 못 넘는다.
- 날짜 칸의 `＋` 로 그 날에 바로 넣는다. **매주 ○요일** 을 켜면 반복이 된다.

### 달력 일정 — 손으로 넣어 운영 달력을 만든다 (2026-09-10)

마왕님 말씀 — *«달력에 일정을 추가해서 업무달력을 만드는 거야. 업무 추가 버튼 누르면
글씨 색깔 지정하고 업무 추가할 수 있도록»*. 부원장님 「중등관 9월 운영 달력」이 그 모양이다.

```
dash/calEvents { items: [{ id, text, from, to, color, grade, created }] }
```

> [!warning] 일정을 할 일에 섞지 않는다 — **여기만은 나누는 것이 맞다**
> 할 일은 한 곳(`dash/tasks`)에 모으는 것이 원칙이지만, 일정은 **다른 것**이다.
> 할 일은 «내가 해야 할 것»이라 담당이 붙고 체크가 붙고 업무 달력에는 **자기 것만** 나온다(2026-09-07 결정).
> 운영 달력은 그 반대다 — «1부 시험»·«추석 연휴»·«본사 워크샵»은 누구의 것도 아니고
> **팀 전체가 같은 것을 봐야 한다.** 할 일에 색 칸만 더했으면 내가 담당이 아닌 일정이
> **남의 화면에서 조용히 사라졌을 것이다.** 그것이 상자를 나눈 이유이고 유일한 이유다.

- **규칙은 있는 것을 그대로 탄다** — `dash` 는 팀 전체가 읽고 **팀장만 쓴다.**
  운영 달력을 만드는 사람이 팀장이라 새 규칙이 필요 없었다.
- **글씨 색 여덟 가지**(`EV_COLORS`) — 기본·검정·빨강·주황·초록·파랑·보라·분홍.
  검정은 부원장님 달력의 마감 칩 그대로다.
- **«까지»를 채우면 막대**가 된다. 시험 막대와 같은 길(`weekSegments`)로 가 주 경계에서 잘린다.
- 넣은 일정을 **누르면 고치거나 지운다.** 달력에서 바로 고칠 데가 없으면 지우고 다시 넣게 된다.
- 넣는 자리 하나에 갈래가 둘이다(`calAddOpen`) — **일정** 과 **할 일**. 기본은 일정.

> [!warning] 손으로 넣은 일정은 **안 묶는다**
> 시험 막대는 겹치면 «가고 외 4» 로 묶지만(`mergeRanges`) 일정은 그러면 안 된다.
> 색과 글자를 사람이 고른 것이라 뭉치면 **무엇을 골랐는지가 통째로 사라진다.**
> 모르는 색 이름이 와도 «기본»으로 그린다 — 색을 못 알아봐서 일정이 안 보이면 안 된다.

> [!warning] 칸은 좁다 — 일정이 먼저다
> 날 칸에 세 줄까지 들어가는데 일정이 자리를 먹으면 할 일이 그만큼 접힌다(`monthDayTasks(d, want, max)`).
> 일정이 이 달력의 본문이고 할 일은 그 위에 얹힌 내 몫이라 그 순서가 맞다.

### 한눈에 들어오게 (2026-09-10)

부원장님 「중등관 9월 운영 달력」(인쇄물 한 장)을 보고 넣었다. 그 종이가 우리 달력보다 잘 읽혔던 이유는
**날 칸에 무슨 일인지가 글자로 있었기 때문**이다. 우리는 숫자만 있었다.

- **날 칸에 할 일이 글자로 뜬다.** 세 개까지, 넘치면 «외 N». 접히는 것은 늘 **덜 급한 쪽** —
  안 끝난 것이 먼저, 그 안에서 마감이 먼저다(`monthDayTasks`).
  한 주의 칸들이 **같은 격자 줄**을 쓰므로 제일 바쁜 날에 맞춰 그 주 높이가 함께 는다.
- **검정 칩 = 마감**(제출·보고·발송·확정·기한), **★ = 개강·설명회·시험**. 그 밖은 학년 색.
  종이 달력의 검정 칩과 ★ 를 그대로 가져왔다 — 먼저 눈에 드는 것이 그 둘이었다.

> [!warning] 새 표시(체크칸)를 달지 않는다 — 글자로 어림잡는다
> 표시를 만들면 **안 단 것이 조용히 밋밋해진다.** 글자 판정은 틀릴 수 있지만
> 틀려도 **할 일은 그대로 보인다** — 강조만 안 될 뿐이다. 빠뜨림과 덜 꾸밈 중에 덜 꾸밈을 고른다.
> 간부회의 메뉴에서 «새 표시를 안 만든» 것과 같은 이유이고, 여기가 더 안전한 쪽이다.

- **쉬는 날은 분홍 칸**이고 이름이 붙는다. `HOLIDAYS` 에 나라 공휴일을 **손으로 적어 둔 것이 기본값**이고,
  **이름을 누르면 학원 사정에 맞게 고친다**(2026-09-10).

> [!warning] 나라 달력이 학원 달력은 아니다
> 연휴에 특강을 열면 쉬는 날이 아니고, 워크샵으로 하루 더 쉬면 나라 달력에 없는 날이 쉬는 날이다.
> 붙박이 목록은 **기본값으로만** 두고, 그 날에 «내가 정한 것»이 있으면 그것이 이긴다.
> `over: true` 가 «이 날은 내가 정한다» 는 표다 — `off` 를 꺼서 **«안 쉼»** 으로도 만든다.
> **지우면 나라 달력대로 돌아간다.** 되돌리는 길이 그것뿐이라 폼에도 그렇게 적어 둔다.
> `off` 만 켠 일정은 `over` 없이도 쉬는 날이 되고, **여러 날짜리면 그 사이를 전부 물들인다.**
> ⚠ 쉬는 날은 **칩으로도 막대로도 안 뜬다.** 칸을 물들이고 날짜 옆에 이름이 붙는 쪽이라
> 칩으로 또 뜨면 같은 말이 한 칸에 두 번 적힌다.
> 다만 **«안 쉼» 으로 고친 것은 보통 일정으로 뜬다** — «연휴에 특강을 연다» 고 적어 놓고
> 그 글자가 어디에도 안 보이면 고친 사람이 자기가 무엇을 적었는지 못 본다.

### 담당을 넣으면 할 일에도 들어간다 (2026-09-11)

마왕님 말씀 — *«업무달력에서 추가하면 할 일에도 자동 등록되는 거지? 그러려면 담당도 넣어야 할 듯»*.
그때까지는 기본 탭 «일정» 에 담당 칸이 없고 할 일에도 안 들어갔다. «할 일» 탭으로 넣어야만 들어갔는데,
기본으로 열리는 쪽이 아니라 헷갈렸다. 이제 **«일정» 폼에 담당 칸이 있고, 채우면 할 일 한 줄이 된다.**

> [!warning] 일정과 할 일을 **두 벌로 두지 않는다** — 담당을 넣으면 그것은 할 일이다
> 일정 사본 + 할 일 사본을 이어 붙이는 길도 있었다. 그러면 한쪽을 고칠 때 다른 쪽이 옛 날짜를 말하고,
> 할 일 메뉴에서 지워도 달력에는 남는다. 그래서 담당이 있으면 `dash/tasks` 에 **한 줄만** 두고,
> 달력은 그 줄을 일정처럼 **색을 입혀 모두에게** 그린다(`allEvents` = 일정 + 달력 업무).
> 끝냄·보고·완료 현황·간부회의가 전부 할 일 줄에서 돌아가므로 따로 이을 것이 없다. 끝나면 달력 칩에 ✓.

```
{ ...할 일, cal: true, color, from?, due }   cal = 운영 달력에 모두가 보는 업무
                                              from ~ due = 여러 날짜리. due 가 마감(할 일 메뉴의 기한)
```

> [!warning] 달력 업무는 **언제나 공개분**이다 (`taskShared`)
> 담당이 팀장 한 사람이면 원래 규칙으로는 팀장 전용 문서(`tasksLead`)로 간다.
> 그러면 모두가 봐야 할 운영 달력에서 **선생님 화면에서만 조용히 사라진다.** `cal` 이면 공개로 보낸다.

> [!warning] 한 칸에 두 번 안 뜨게
> 담당에게는 달력 업무가 **할 일이기도 하다.** 일정 칩으로 뜨는데 할 일 칩으로도 세우면 같은 줄이 둘이다.
> 날 칸·주간 칸의 할 일 칩에서는 `cal` 을 빼고, **«이번 주까지 해야 할 일» 목록에는 남긴다** — 거기는 칸이 아니라 내 목록이다.

- **갈래가 바뀌면 옛 쪽을 치운다**(`applyCalSave`) — 일정에 담당을 넣으면 일정에서 빠지고 할 일이 되고,
  담당을 비우면 할 일에서 빠지고 일정이 된다. 그때는 끝냄·보고 표시가 사라지므로 **한 번 묻는다.**
- 저장은 **할 일부터** 쓴다. 일정→할 일로 옮기다 일정 쪽 쓰기가 실패해도 할 일은 남아 잃는 것이 없다.
- **쉬는 날은 담당을 안 받는다**(`calSavePlan`). 누구의 일도 아니다. «쉬는 날» 을 켜면 담당 칸이 잠긴다.
- «할 일» 탭은 남겼다 — **매주 반복**과 «담당에게만 보이는 할 일»은 거기서 넣는다.

### 달력에 뜨는 것은 전부 달력에서 고친다 (2026-09-11)

마왕님 말씀 — *«업무달력에 있는 모든 일정을 수정할 수 있게 해봐»*. 달력에 뜨는 것은 다섯 가지고 전부 눌러서 고친다.

| 달력에 뜨는 것 | 누르는 곳 | 고치면 바뀌는 곳 |
|---|---|---|
| 일정 | 칩 · 막대 | `dash/calEvents` |
| 쉬는 날 | 날짜 옆 이름 | `dash/calEvents` (`over`) |
| 할 일 | 월간 칩 · 주간 칸 글자 · 이번 주 목록 글자 | 할 일 메뉴와 **같은 줄** (`saveTaskOf`) |
| 시험 | 시험 막대 | 시험 성적의 **그 시험** `tests/{tid}` (합쳐 쓴다) |
| 반 종강 | 종강 막대 | 수업관리 앱의 **그 반** `classes/{cid}.endDate` |

> [!warning] 사본을 두지 않는다 — 고친 것이 **원래 자리에 바로** 들어간다
> 9/10 에는 «근거지가 둘이 된다» 며 시험·종강을 막아 뒀다. 그 걱정은 달력에 **따로 적어 둘 때**의 이야기다.
> 같은 문서를 다른 문으로 고치는 것은 근거지가 늘지 않는다. 그래서 막대가 `ref`(시험 tid · 반 cid)를 들고 다닌다.
> 겹쳐서 한 막대로 묶인 것(«중간 가고 외 1»)은 누르면 **무엇을 고칠지 먼저 고른다**(`m.refs`).

> [!warning] 반 종강일은 수업관리 앱의 값이다
> 여기서 바꾸면 앱에서도 바뀐다. `update({ endDate })` 로 **그 칸 하나만** 쓴다 — `set` 이면 명단(roster)이 날아간다.
> 오늘보다 앞선 날로 하면 `activeClass` 가 끝난 반으로 잡아 반 목록·배치표·참여표에서 빠진다. 그래서 그때만 한 번 더 묻는다.
> 비우면 종강 없는 반이다.

- **시험은 여기서 못 지운다.** 지우면 점수까지 같이 지워지므로 시험 성적 메뉴에서만 한다. 이름·종류·날짜·학년만 고친다.
- **할 일은 글자·기한·학년·담당·매주를 고치고 지운다.** 할 일 메뉴에는 글자·날짜를 고칠 자리가 없어서
  **여기가 처음이자 유일한 자리**다. 한 번 ↔ 매주로 바꿀 때 옛 칸을 치운다(`applyTaskFix`) —
  매주로 바꿨는데 `due` 가 남으면 할 일 메뉴의 기한 칸이 옛 날짜를 계속 말한다.
  자기가 적은 할 일은 담당 칸을 안 준다 — 담당은 문서 주인이 정한다(`ownTasks`). 줘 봐야 도로 돌아온다.
  **체크칸은 따로 논다.** 글자를 누른 것만 고치기로 가고 체크칸은 지금처럼 끝냄/안 끝냄이다.
- **권한은 원래 자리의 것을 따른다.** 선생님은 **자기가 적은 할 일만** 고친다(`canEditTask`).
  시험·종강·일정·쉬는 날은 팀장 것 — 규칙도 그렇게 막는다(tests 는 owner, classes 는 `teaches(cid)`, dash 는 owner).
  선생님 화면에 뜨는 폼은 `data-keep` 을 달아야 «감추기»(`applyReadOnly`)가 칸을 안 잠근다.

> [!warning] 공휴일을 셈으로 내지 않는다
> 해마다 **대체공휴일**이 붙어 요일에 따라 달라진다. 틀린 날을 자신 있게 그리느니
> **안 적힌 해는 아무것도 안 그리는 편**이 낫다 — 빈 칸은 «아직 안 적었다» 로 읽히지만
> 틀린 빨간 칸은 아무도 의심하지 않는다. 안 적은 해를 보고 있으면 머리 줄이 그렇게 말한다(`holidayKnown`).
> 2026 은 부원장님 달력(9/24~26 추석 · 10/3·10/5 · 10/9)과 맞춰 봤다. **2027 은 아직 안 적었다.**

- **머리에 한 줄** — 이 달 할 일 · 기한 지남 · 마감 · 끝냄 · 쉬는 날(`calMonthSum`).
  달을 넘기기 전에 이것부터 본다.
- **범례**를 달력 위에 둔다. 색이 뜻하는 것을 아무도 안 물어봐도 되게.
- **인쇄** 단추 — 이 달만 **A4 가로 한 장**으로 나간다(`@media print`).
  종이 달력이 원본이었으니 종이로도 나가야 한다. 배경색을 안 찍으면 쉬는 날도 마감 칩도 흰 종이가 되므로
  `print-color-adjust: exact` 를 준다.

### 시험

```bash
node tools/test-cal.js
```

196건. 주 계산(일요일 포함), 월간 격자와 달 옮기기, 어느 날에 놓이는가, 반복의 날짜별 완료,
이번 주까지 목록, 학년 거르기, **막대 묶기**(겹침·사슬·종류), **막대 눕히기**(주 경계·층),
**쉬는 날**(추석·대체공휴일·안 적은 해), **마감·★ 판정**, **칸 안의 글자**(세 개까지·접히는 순서), **머리 한 줄**,
**달력 일정**(하루/기간·색·학년·안 묶임·선생님에게도 보임·일정이 할 일 자리를 먹는 것),
**쉬는 날 고치기**(이름 바꾸기·«안 쉼»으로·없던 날 만들기·여러 날·지우면 되돌아감),
**전부 고치기**(막대가 문서 id 를 든다·묶여도 남는다·시험은 tests/{tid} 에 합쳐 쓴다·종강은 endDate 한 칸만·
선생님은 시험·종강을 못 고친다·한 번↔매주·자기 할 일만 눌린다),
**담당 넣기**(담당이면 할 일·쉬는 날은 안 받음·공개분으로 저장·모두의 달력에·두 번 안 뜸·✓·갈래 바꾸면 옛 쪽을 치움).
`tools/test-ro.js` 는 **선생님이 일정을 못 쓰는 것**을 본다.

### 학생 PIN 초기화 (2026-09-05)

앱의 학생 명단에 있는 것과 같은 단추를 명단 편집 표에 넣었다. **새 비번을 정해주지 않고**
초기 PIN 으로 되돌린다 — 선생님이 정한 비번은 그것대로 남의 손을 거친 비번이 된다.
학생이 다음에 들어올 때 스스로 정한다.

- `stResetPin(name)` → `classAuthApi({ action: "resetStudentPin", name })`
- **수업관리 앱의 `/api/auth` 를 브라우저에서 바로 부른다.** 그쪽은 CORS 를 열어 두었고
  class 토큰으로 스스로 권한을 본다 — 우리 서버가 한 번 더 감쌀 이유가 없다.
  (학교 일정은 CORS 가 없어서 서버가 중계한다. 두 경우가 다르다)
- 넘기는 것은 **class 토큰**이다. team 토큰으로는 그쪽이 아무것도 안 해준다.
- PIN 은 이름 하나에 하나뿐이라 **동명이인은 초기화할 수 없다.** 그쪽 서버가 409 로 막고
  "이름을 A·B·C 로 나눈 뒤 다시" 라고 알려준다. 그 말을 그대로 보여준다.

## 함정

- 앱 데이터의 칸 이름은 수업관리 앱 CLAUDE.md 가 원본이다. 특히 학생 명단의 담임은 `students.homeroom`(tid),
  roster 의 `teacher` 는 "이 항목이 선생님"이라는 불리언. 앱이 칸을 바꾸면 여기가 조용히 빈다.
- 상자 일곱 개를 `render*()` 가 각각 innerHTML 로 다시 그린다. 상자를 통째로 다시 그리면 입력 포커스가
  날아가므로 시트 찾기 칸은 표만 다시 그린다(`renderSheetTable`).
- 성적은 반마다 `days` 문서를 모두 나열한 뒤 최근 14개 날짜만 `scores` 를 읽는다. 반이 늘면 느리다.
  앱 쪽에 `collectionGroup("scores")` 규칙을 열면 한 번에 되지만 그건 앱 규칙을 건드리는 일이라 안 했다.
- 시트 id 는 저장소에 적지 않는다(공개). 대시보드 안에서 주소를 붙여넣어 `dash/config` 에 둔다.
- 구글시트는 시트마다 서비스 계정 이메일을 **뷰어**로 공유해야 한다. 처음엔 구글 클라우드 콘솔에서
  climath-team 의 Google Sheets API 를 켜야 한다. `api/sheets.js` 의 `explain()` 이 그 링크를 돌려준다.
- 로컬에서 켜볼 수 없다. `/api` 가 Vercel 에만 있고 Firestore 는 로그인이 있어야 열린다.
  구문 검사: `node --check api/*.js`, index.html 은 `<script>` 를 뽑아 `new vm.Script()`.
  **그리기 함수는 가짜 데이터로 돌려볼 수 있다** — `<script>` 를 vm 컨텍스트에 넣고(firebase·document·
  location 은 빈 껍데기로) `S` 에 반·학생을 손으로 채운 뒤 `classGridHtml()` 같은 걸 불러 HTML 을 본다.
  브라우저 없이 배치표가 실제로 그려지는지 확인하는 유일한 방법이다.

## 볼트

작업 노트: `민수의 뇌/20 작업/한민수 대시보드.md` — 켜는 순서, 연동 후보 시트 목록.

## 새로 온 할 일 — 앱 안 알림 (2026-09-08)

«할 일에 내용이 추가될 때마다» 알린다. **앱 안에서만** — 머리띠의 종에 숫자, 목록의 그 줄에 «새로 옴».
앱을 닫아둬도 오는 푸시(FCM)는 아직 아니다. 그건 서비스 워커·VAPID 키·발송 계기가 더 필요하고,
**아이폰은 홈 화면에 추가해야만** 온다 — 선생님 일곱에게 그걸 시키기 전에 앱 안 알림부터 세웠다.

```
marks/{tid}.seenTasks     : ["할일id", ...]   ← 그 사람이 어디까지 봤나 (기기가 바뀌어도 따라온다)
localStorage["dash.seen.<tid>"]              ← 같은 것을 이 브라우저에도. 오프라인에서 눌러도 살아난다
```

- **새 컬렉션을 안 만들었다.** 만들면 규칙을 콘솔에 다시 붙여넣어야 하는데, `marks` 는 이미
  «자기 문서만 쓴다» 로 열려 있다(팀장은 아무 문서나).
- **읽을 때 둘을 합친다**(`initNewTasks`). 한쪽에서만 확인했어도 다시 안 울린다.
  팀 DB 만 쓰면 오프라인에서 누른 것이 되살아나고, 브라우저만 쓰면 폰과 PC 가 따로 센다.
- `bellTasks()` 가 세는 대상 — **내가 넣은 줄은 뺀다**(`MY_NEW`). 팀 할 일은 팀장만 넣을 수 있어서,
  이걸 안 빼면 팀장이 할 일을 넣을 때마다 자기 종이 울린다. 남의 화면에서는 그대로 «새로 옴» 이다.
  할 일을 만드는 자리는 `newTaskId()` 하나로 모았다 — `newId()` 를 그냥 쓰면 그 줄이 남이 넣은 것과 구별되지 않는다.
- **남이 자기 앞으로 적은 할 일**(`own`)은 팀장만 센다. 선생님끼리 개인 메모를 알리면 소음이고,
  팀장에게는 «누가 무엇을 자기 몫으로 걸었나» 가 볼 값이 있다.
- 적어 두는 목록(`seenIds()`)에는 **내가 만든 것까지** 넣는다. 안 그러면 다음에 열 때 그 줄이 «새로 옴» 으로 뜬다.
- `initNewTasks()` 는 `loadCore` 가 끝난 뒤 `boot()` 에서 한 번 돈다.

### 지켜보기 (2026-09-08 오후)

처음엔 **다시 열 때만** 셈했다. 그러면 팀장이 할 일을 넣어도 선생님 화면은 그대로다 — 알림이라 부를 수 없다.
`watchTasks()` 가 `onSnapshot` 으로 세 곳을 지켜본다: `dash/tasks` · `dash/tasksLead`(팀장만) · `myTasks`.
바뀌면 `rebuildTasks()` 가 그 한 곳만 갈아 끼우고 셋을 다시 합친 뒤 종·할 일·달력·2주를 다시 그린다.

> [!warning] 내 쓰기에도 곧바로 한 번 돈다
> Firestore 는 서버 답을 기다리지 않고 먼저 알려준다(latency compensation). 그래서 `MY_NEW` 로
> 내가 만든 줄을 빼 두지 않으면 **할 일을 넣는 순간 자기 종이 울린다.**

> [!warning] `S.tasks` 는 세 곳을 합친 것이다
> 공개분(`S.tasksOpen`) · 팀장분(`S.tasksLead`) · 각자분(`S.own`). 지켜보기가 통째로 갈아치우면
> 나머지 둘이 사라진다. 그래서 공개분을 `S.tasksOpen` 에 따로 들고 있는다.

> [!warning] 팀장 전용 문서에 선생님이 지켜보기를 붙이면 안 된다
> `dash/tasksLead` 는 규칙이 막는다. 붙이면 오류만 계속 난다. `S.ro` 면 건너뛴다.
### 오프라인 · 누르기 전까지 (2026-09-08 저녁)

마왕님 요청 — «오프라인이어도 종에 추가되어야 하고, 누르기 전까진 안 없어져야 한다».

- **종은 종을 눌러야 꺼진다.** 전에는 할 일 «화면을 열면» 꺼졌다. 이 앱은 마지막에 보던 메뉴를
  기억했다가 다시 여니, 할 일에서 닫은 사람은 다음에 켜는 순간 종이 저 혼자 꺼져 무엇이 왔는지 못 봤다.
  이제 `markTasksSeen()` 을 부르는 자리는 **종 버튼과 종 안의 줄** 둘뿐이다.
  «새로 옴» 표(`S.newIds`)는 그 판까지 남긴다 — 종만 끄고 표까지 지우면 목록에서 못 찾는다.
- **오프라인이어도 안 잃는다.** 종은 «지금 있는 id» 와 «봤다고 적어 둔 id» 의 차이다. 그 사이에
  연결이 끊겼다 붙어도 차이는 그대로 남는다. 끊긴 동안 온 것이 나중에 한꺼번에 뜬다.
- `tdb.enablePersistence({synchronizeTabs:true})` — 팀 DB 를 캐시에 남긴다. 지하철에서 열어도
  마지막에 본 할 일과 종이 그대로 뜨고, 오프라인에서 «봤다» 고 적은 것이 줄에 섰다가 연결되면 올라간다.

> [!warning] 수업관리 DB(`db`)는 캐시를 켜지 않는다
> 켜면 학생 이름·학교·성적이 브라우저 IndexedDB 에 통째로 남는다. 학원 공용 PC 에서 로그아웃만으로는
> 안 지워진다. 알림에 필요한 것은 팀 DB 뿐이다. `enablePersistence` 는 **다른 읽기·쓰기보다 먼저**
> 불러야 하고, 창을 여럿 띄웠거나 브라우저가 안 되면 실패한다 — 실패해도 앱은 그대로 돌게 catch 한다.

> [!warning] 오프라인 쓰기 큐는 새로고침하면 날아간다
> 팀 DB 쓰기는 연결이 없으면 줄에 선다. 그 사이에 새로고침하면 큐가 사라져 **이미 확인한 알림이
> 되살아난다.** 그래서 `saveSeen()` 이 팀 DB 와 `localStorage` 두 군데에 적는다.

> [!warning] 처음 들어온 사람에게 지난 할 일을 쏟지 않는다
> `seenTasks` 칸이 **아예 없으면** 지금 것을 기준선으로 적어두고 조용히 넘어간다.
> 안 그러면 선생님이 처음 여는 날 마흔 건이 전부 «새로 옴» 으로 뜬다.

> [!warning] `set()` 은 문서를 통째로 갈아친다 — «내 완료»가 «어디까지 봤나»를 지운다
> `toggleMyMark` 가 `{done, name, updated}` 만 넘기고 있었다. 그대로 두면 체크 한 번에 `seenTasks` 가 날아가
> 다음에 열 때 할 일 전체가 «새로 옴» 이 된다. `Object.assign({}, cur, {...})` 로 있던 칸을 얹는다.
> `tools/test-bell.js` 가 이걸 지킨다.

### 시험

```bash
node tools/test-bell.js
```

44건. 기준선·늘어난 것만 세기·지워진 id 걸러내기·«내 완료»가 안 지우는지·그린 종,
**내가 넣은 줄이 내 종을 안 울리는지**·팀장과 선생님이 다르게 세는지·지켜보기가 그 자리에서 도는지,
그리고 **할 일 화면을 열어도 안 꺼지는지**·오프라인에서 누른 것이 살아나는지·폰과 PC 기록을 합치는지.
`localStorage` 는 흉내가 아니라 **진짜로 들고 있는 것**을 넣는다 — getItem 이 늘 null 이면 이 셋을 못 본다.

## 끝낸 것은 아래로 (2026-09-08)

마왕님 말씀 — "할일에서 내 완료 클릭한거나 보고한거는 아래로 뜨게 만들어".
목록의 상태는 팀장만 바꾸므로 선생님이 «내 완료» 를 눌러도 줄이 열린 것 사이에 그대로 서 있었다.

`taskRank`: **0 아직 할 것 → 1 내가 끝낸 것 → 2 팀장이 닫은 것**(«완료 보기» 를 켜야 보인다). 그 안에서 기한순.
단계가 바뀌는 자리에 `.tsep` 가르는 줄(«내가 끝낸 것» · «팀장이 닫은 것»)이 선다 — 내려갔다는 것이 보여야 «사라졌다» 로 안 읽힌다.

- **«내가 끝낸 것» 은 보는 사람 기준**(`taskSettled`): 내 `marks.done` 이나 `reports` 에 있으면. 남이 끝낸 것은 내게는 아직 할 것이다.
- **팀장에게는 «걸린 사람이 모두 끝낸 것»** 이 그것이다. 남은 사람이 하나라도 있으면 위에 남는다.
- 시험은 `tools/test-report.js` 의 «끝낸 것은 아래로» 묶음 (7건).

## 숫자 상자를 누르면 그것만 (2026-09-08)

"기한지남 빨간색으로 표시되는데, 누르면 뭔지도 표시하자". 할 일 위의 숫자 상자 넷(기한 지남·이번 주·내 것·담당 없음)이
단추다 — `S.statPick`. 누르면 목록이 그것만 남고 «N건만 보인다 · 전부 보기» 한 줄이 뜬다. 같은 상자를 다시 누르면 푼다.
담당 칩(`S.filterWho`)과는 **둘 다 만족하는 것**만. 상자의 숫자는 걸러도 전체 수 그대로다 — 숫자가 따라 줄면 무엇을 눌렀는지 헷갈린다.
시험은 `tools/test-report.js` 의 «숫자 상자를 누르면 그것만» 묶음.

## 한 줄 보고 (2026-09-08)

어떤 일은 «했다» 만으로는 부족하다 — **무엇을 했는지 한 줄**이 있어야 다음을 정한다.
할 일에 `report: true` 를 달면 그 줄의 «내 완료» 자리가 **적는 칸**으로 바뀐다.

```
marks/{tid}.reports[할일id] = { text: "1반·2반 다 돌렸습니다", at: "2026-09-08" }
```

- **켜고 끄는 것은 팀장.** 줄의 «보고» 표를 누르거나, 새 할 일 칸의 «보고 받기» 를 체크한다.
  선생님에게는 못 누르는 표로만 뜬다 — «이 일은 글로 보고한다» 는 표시다.
- **보고가 곧 완료다.** 적어 보내면 `done[할일id]` 도 같이 찍힌다. 글을 비우면 완료도 내려간다 —
  글 없는 «했음» 만 남으면 무엇을 했는지 영영 모른다.
- 팀장 줄 밑에 들어온 보고가 그대로 붙고(`reportsSeen`), **완료 현황에도 같은 글**이 뜬다(«보고» 표).
- 선생님에게는 남의 보고가 안 보인다. 자기 칸만 적고 자기 글만 본다.

> [!warning] `marks` 안에 둔다 — 새 컬렉션을 만들지 않는다
> 만들면 규칙을 콘솔에 다시 붙여넣어야 하는데 `marks` 는 이미 «자기 문서만 쓴다» 로 열려 있다.
> 종의 `seenTasks` 와 같은 이유다. `set()` 이 문서를 통째로 갈아치므로
> `done`·`seenTasks` 를 `Object.assign` 으로 얹는다 — **보고 한 번에 그것들이 날아간다.**

> [!warning] 못 썼으면 화면 사본도 되돌린다
> 적힌 척하면 선생님은 보고했다고 믿고 팀장은 영영 못 본다. `saveReport` 의 catch 가 `S.marks` 를 되돌린다.

> [!warning] 적는 도중에 다시 그리면 글이 날아간다
> `marks` 지켜보기는 **남이 보고할 때마다** 돈다. 그때 `renderTasks()` 를 그냥 부르면
> 내가 치던 글이 통째로 사라진다. `renderTasksSafe()` 가 `#sec-tasks` 안의 칸에 손이 올라가 있으면
> `TASKS_STALE` 만 켜고 미룬다. 손을 뗄 때(`blur`) 그린다.
> 단추·칸에 `data-keep` 이 없으면 `applyReadOnly` 가 «완료» 글자를 보고 선생님 화면에서 숨긴다.

```bash
node tools/test-report.js
```

43건. 저장·삭제·되돌리기, 다른 칸을 안 지우는지, 읽기 계정의 문이 다시 닫히는지,
칸이 뜨는 자리, 팀장·선생님이 다르게 보는 것, 적는 중에 안 그리는지, 완료 현황에 글이 따라오는지.

## 시트와 명단 대조 (2026-09-08)

데스크의 «인원 현황» 시트(`[2609] 대치CMS 중*고등관 인원 현황`)와 앱 명단을 맞춰 보고 **다른 곳만** 팀장에게 보인다.
마왕님 말씀 — "스프레드시트 명단이 바뀐다고 학생 명단이 휙휙 바뀌면 안 돼. 달라진 부분이 있는지 확인하고
알림으로 알려줘. 내가 그걸 보고 바꿀지 말지 결정한다."

> [!warning] 여기서는 아무것도 저절로 바꾸지 않는다
> 대조는 읽기만 한다. 차이를 줄로 세우고, 줄마다 «반영»·«무시» 를 팀장이 누른다.
> `tools/test-roster-sheet.js` 가 «대조만으로는 명단에 아무것도 안 쓴다» 를 센다.

### 어디서 도나

- **팀장이 앱을 열 때**(`boot` → `checkRosterSheet`). 시트를 읽고 앱 명단(`S.classes[].roster`)과 맞춰 본다.
  선생님 계정은 시트를 부르지도 않는다.
- 다르면 **종**에 «명단이 시트와 다르다 N건» 한 줄이 실린다. 누르면 학생 명단으로 간다.
- 학생 명단 맨 위 노란 상자가 대조 결과다. «다시 대조» 로 그 자리에서 또 읽는다.
- 마지막 대조가 언제였고 몇 건이었는지만 `dash/rosterDiff` 에 남긴다 — **학생 이름은 팀 DB 에 안 적는다.**

> [!note] «매일 아침 10시» 는 아직 서버 크론이 아니다
> 팀체크의 서버(팀 서비스 계정)는 수업관리 앱 DB(학생·반)를 못 읽는다. 진짜 10시 크론은 **수업관리 앱 쪽**에
> 두거나 그 서비스 계정에 권한을 줘야 한다. 지금은 팀장이 여는 순간 맞춰 보는 것이라, 앱 안 알림으로는 결과가 같다.
> 붙이게 되면 `dash/rosterDiff` 에 같은 모양(`at, sig, n, tab`)으로 적으면 된다.

### 시트 모양 (26년 9월 예상 탭)

반이 **열**, 학생이 **줄**이다. C열이 «반» 인 줄이 덩어리의 머리다.

```
        D열~      … 반 이름은 "고1S\n(201호)" 처럼 줄바꿈·호수가 섞인다
요일    월금 월금 토
반      고1S 고1T 고1자사
담임    한민수 …
재원 1  김서진10   ← 이름 + 학년 숫자 (7=중1 … 12=고3). 앞의 N 은 이번 달 신입, 뒤의 a·b·A 는 동명이인
     2  …
신입예정 1 N박준호9  ← 아직 안 온 사람. 예고로만 보인다
퇴원       정하늘9  ← 나가는 사람. 예고로만 보인다
(빈 줄)
요일    화목 …       ← 둘째 덩어리. 요일별로 셋(월금 / 화목 / 다른 관)
```

오른쪽에 FP 강좌 격자가 **같은 줄을 나눠 쓴다**(«강좌명» 열). 그 열의 «재원»·«신입예정» 글자를 이름으로 읽으면
안 되므로 `SHEET_WORDS` 로 거른다.

### 고등부 반만 본다 (2026-09-08)

마왕님 말씀 — "명단 대조하는거는 다른반은 다 빼고 고등부 반만 가져오자" 하시며 열 열 개를 짚어 주셨다:
고1S · 고1T · 고1 자사 토1부 · 예비고1 S/T/A(월금 덩어리) · 고1TOP · 예비고1 S/T/A(화목 덩어리).
시트에는 그 옆으로 고PL·중PL(자습), 1GS·2GS·UGS(중등관 진도반), FP 강좌, 고3M 수1 이 있고
셋째 덩어리(다른 관)에 «예비고1S 월금» 이 또 있다.

`highCols` — 규칙 둘. **반 이름 열쇠가 `HIGH_CLASS`(`/^(예비)?고1/`)에 걸리고, 덩어리 번호가 `HIGH_BLOCKS`(2) 앞**인 열만.
`parseRosterSheet` 가 열마다 `block` 을 남긴다.

> [!warning] 셋째 덩어리의 «예비고1S» 는 이름으로는 못 뺀다
> 월금 덩어리의 «예비고1 S(404호)» 와 열쇠가 같다(`예비고1S`). 덩어리 번호로 빼지 않으면 한 사람이 «반 둘에 있다» 로 보이고
> 앱 반 하나에 시트 열 둘이 붙는다. 고PL 은 «고» 뒤에 숫자가 없어, 고3M 수1 은 고1 이 아니라 이름만으로 빠진다.

- 걸러낸 열은 **읽지도 맞추지도 않는다** — «어느 반인지 모른다» 에도 안 선다. 상자 머리에 «고등부 N반».
- 고등부 열이 하나도 없으면 실패로 적힌다(«같다» 로 보이면 안 된다).
- 고2·고3 반이 팀에 들어오면 `HIGH_CLASS` 를 넓힌다.
- `node tools/peek-roster-cols.mjs` — 진짜 시트의 «반» 줄과 앱이 보는 고등부 열을 찍는다. **학생 이름은 안 찍는다.**

### 시트 열 ↔ 앱 반

`matchSheetCols`. 손으로 정한 것(`config.rosterMap[label|요일]`)이 먼저, 없으면 이름 열쇠로:
`normClassKey` 가 줄바꿈·빈칸·괄호(호수)·끝의 «반» 을 떼고 대문자로 만든다.
**요일까지 붙인 열쇠**("예비고1S화목")가 맞으면 그것을, 안 맞으면 요일 뗀 열쇠("고1S")로 본다.

> [!warning] 둘 이상 맞으면 안 맞춘 것으로 둔다
> 앱에 «예비고1S 월금»·«예비고1S 화목» 이 다 있는데 시트 열에 요일이 없으면 둘 다 맞는다.
> 이럴 때 아무거나 고르면 반 하나가 통째로 «퇴원» 이 된다. 상자에 고르는 칸이 뜨고, 고른 것은 `rosterMap` 에 남는다.
> 「앱에 없는 반」으로 고른 것도 기억한다(빈 문자열).

### 차이의 종류

| 줄 | 뜻 | «반영» 이 하는 일 |
|---|---|---|
| 신입 | 시트에 있는데 앱에 그 사람이 아예 없다 | 사람을 만들고 반에 넣는다. **학교는 비운다** — 시트에 없다. 명단 화면이 채우라고 알린다 |
| 배정 | 사람은 있는데 그 반에 없다 | 반에 넣는다 |
| 전반 | 앱은 이 반, 시트는 저 반 | 옛 반에서 빼고 새 반에 넣는다. `stUnassign` 이 번호를 `pastIds` 에 적어 **기록이 이어진다** |
| 퇴원 | 앱 그 반에 있는데 시트 어디에도 없다 | **반에서 뺀다. 사람 문서는 남긴다** |
| 확인 | 같은 이름이 앱에 둘 이상 | 반영 단추가 없다. 손으로 본다 |

> [!warning] 이름으로 맞춘다 — 그래서 동명이인이면 손을 뗀다
> 시트에 학생 번호가 없다. 같은 이름이 둘이면 «확인» 줄로만 세우고 어느 쪽에도 안 붙인다.
> 동명이인을 섞어 남의 기록에 붙이면 되돌리기 어렵다.

> [!warning] 퇴원은 지우기가 아니다
> `stDelete` 는 여기서 **절대** 안 부른다. 출결·과제·질문·참여표가 그 번호에 달려 있고, 다시 오면 번호를 되써야 이어진다.
> 시험이 «퇴원이 사람 문서를 지우지 않는다» 를 센다.

- 맞춘 반만 본다. 시트에 없는 앱 반(고1T 같은)의 학생은 «퇴원» 이 아니다 — 서로 할 말이 없다.
- 한 사람이 시트에서 반 둘에 있으면 «전반» 이 아니라 «배정» 이다. 전반은 «앱 반 하나 ↔ 시트 반 하나» 일 때만.

### 종과 «봤다»

차이의 **지문**(`sigOf` — 줄 열쇠를 정렬해 해시) 하나로 «어디까지 봤나» 를 센다. 같은 차이면 다시 안 울리고,
한 줄이라도 바뀌면 다시 울린다. 종을 누르면 `marks/<tid>.seenDiff` 와 `localStorage` 에 지문이 남는다(할 일과 같은 두 군데).

- «무시» 는 `config.rosterIgnore[열쇠]` 에 남는다. **시트에서 그 차이가 사라지면 잊는다** — 그 사람이 나중에 다시 오면 다시 물어야 한다.
  시트를 못 읽은 판에는 무시 목록을 안 건드린다(빈 시트로 전부 잊으면 안 된다).
- 못 읽으면 상자에 빨갛게 적힌다. **조용히 «같다» 로 보이면 안 된다** — 그건 안 본 것이지 같은 것이 아니다.

### 근거 자료

`SOURCES` 의 `roster` 항목. 주소에 `#gid=` 가 있으면 그 탭, 없으면 첫 탭(`sheetGidFrom`). 탭은 달마다 바뀌므로
(«26년 9월 예상» → «26년 10월 예상») **달이 바뀌면 근거 자료에서 주소를 다시 붙인다.**
`api/sheets.js` 의 `meta` 가 탭마다 `gid` 를 돌려준다.

브라우저 로그인 없이 등록하려면 `node tools/register-roster-sheet.mjs "<시트 주소>"` — 팀 서비스 계정 열쇠로 읽히는지 먼저 보고 적는다.

```bash
node tools/test-roster-sheet.js
```

87건. 칸 읽기(학년 숫자·N·동명이인 꼬리·구역 말 거르기), 반 이름 열쇠, 덩어리 셋·FP 격자, 열 맞추기(요일·손으로 정한 것·둘 이상이면 손 떼기·종강 반 제외),
차이 다섯 종류, 지문·무시, 읽어서 맞춰 보기(gid·실패·시트 없음), 선생님은 안 도는지, 종, **대조만으로는 아무것도 안 쓰는지**·퇴원이 안 지우는지·반영 넷, 그린 것.

## 완료 현황 — 팀장만 보는 메뉴 (2026-09-08)

강사들이 **무엇을 끝냈는지** 사람별로 모은다. 할 일 목록은 «남은 것» 을 보는 자리라 끝난 것은
접혀 있고 사람별로도 안 모인다 — «이번 주에 누가 무엇을 했나» 를 볼 데가 없었다.

`PAGES` 의 셋째 칸이 `"owner"` 인 첫 메뉴다. 선생님에게는 메뉴도 칸(`#sec-done`)도 안 생기고,
주소에 `#done` 을 쳐도 `isPage()` 가 막아 할 일로 보낸다.

> [!warning] 메뉴를 감추는 것은 화면 정리지 자물쇠가 아니다
> 팀장만 볼 **자료**라면 규칙으로도 막아야 한다. 완료 현황이 쓰는 `marks`·`myTasks` 는
> 원래 팀 전체가 읽는 것이라(누가 끝냈는지 서로 본다) 여기서 새로 여는 것이 없다.
> **모아 보는 화면만** 팀장 것이다.

### 끝났다는 표시가 두 가지다

| 어디 | 무엇 | 화면 표 |
|---|---|---|
| `marks/<tid>.done[할일id]` | 팀장이 시킨 일에 본인이 «내 완료» 를 누른 것 | 시킨 일 |
| 〃 + `reports[할일id]` | **한 줄 보고**까지 적어 보낸 것 — 글이 같이 뜬다 | 보고 |
| `myTasks/<tid>.items[].status === "done"` | 자기가 적고 자기가 끝낸 것 | 직접 적은 것 |

둘을 같이 세되 **어느 쪽인지 남긴다.** 섞어 버리면 «시킨 일을 했다» 와 «자기 할 일을 적었다» 가
같은 숫자가 되어, 숫자가 사람을 잘못 말한다.

### 숫자가 사람을 잘못 말하지 않게

- **0건인 사람도 세운다.** 담당 칩의 다섯(`WHO_CHIPS`)은 한 것이 없어도 칸이 뜬다.
  안 한 사람이 목록에서 사라지면 «아무도 안 했다» 가 «아무 일도 없었다» 처럼 보인다.
- 0건 칸에 **왜 비었는지** 적는다 — «맡은 일이 N건 남아 있다» 와 «맡은 일도 없다» 는 다른 얘기다.
- 사람마다 **남은 일**과 **기한 지남**을 같이 띄운다. 한 것만 보면 많이 맡은 사람이 불리하다.
- **지워진 할 일의 표시도 센다.** 그 줄을 버리면 한 것이 조용히 줄어든다. «지워진 할 일» 로 적는다.
- 아무도 «내 완료» 를 안 누른 채 닫힌 팀 할 일은 **따로 접어** 보여준다. 이걸 감추면
  사람별 숫자가 «아무도 안 했다» 로 읽히는데, 실은 팀장이 목록에서 바로 완료로 바꾼 것들이다.
- «전체» 기간에서는 **날짜 없는 것도 넣는다.** 옛 할 일에는 끝낸 날이 없어서, 날짜로만 거르면
  어느 기간에도 안 나와 통째로 사라진다.

### 지켜보기

`watchTasks()` 에 `marks` 를 붙였다.

> [!warning] 지켜보지 않으면 완료 현황은 로그인한 순간 그대로 멈춘다
> 강사가 지금 «내 완료» 를 눌러도 팀장 화면은 새로고침 전까지 «안 했다» 로 보인다.
> 화면은 멀쩡하고 숫자만 옛것이라 아무도 이상하게 여기지 않는다. 할 일의 `2/3` 도 같은 문제였다.
> 종에서 이미 한 번 밟은 함정이다 — **«현황»은 지켜보기가 있어야 현황이다.**

종과 달리 여기서는 **내가 누른 것도 센다.** 이건 «새로 온 것» 이 아니라 «누가 무엇을 끝냈나» 다.

```bash
node tools/test-done.js
```

42건. 기간 넷, 두 가지 표시를 다 세는지, 지워진 표시가 안 사라지는지, 0건인 사람이 서는지,
남은 일·기한 지남 셈, 표시 없이 닫힌 것이 사람 숫자에 안 섞이는지, 그린 것(div 균형·사람 고르기),
그리고 **marks 스냅샷이 오면 그 자리에서 다시 그리는지**.

## 간부회의 준비 — 팀장만 보는 메뉴 (2026-09-10)

"간부회의까지 준비해야 할 것들을 모아서 보고 싶다. 회의록에서 업무를 정리해서 할 일에 들어가는데,
간부회의에 관련된 것만 따로 모아보고 싶다. 관리자인 나한테만 들어오는 메뉴여야 한다."

`PAGES` 의 `"owner"` 메뉴 둘째(`exec`). 선생님에게는 메뉴도 칸도 없고 `#exec` 를 쳐도 할 일로 보낸다.
여기서 **새로 여는 자료는 없다** — 읽는 `tasksLead`·간부 회의록은 규칙이 이미 팀장에게만 연다. 모아 보는 화면만 팀장 것이다.

### 어느 할 일이 간부회의 것인가 — 새 표시를 달지 않는다

할 일은 이미 어디서 왔는지를 들고 있다. 그 셋으로 가른다(`execWhy`). 표시를 새로 만들면 안 단 것이 빠지고, 빠진 것은 화면이 말해 주지 않는다.

| 칸 | 값 | 어디서 온 것 |
|---|---|---|
| `from` | 간부 회의록 id (`isExecMinute` — 종류·제목에 «간부») | 할 일 화면의 «회의록에서 끌어오기» |
| `src` | «간부 · 9/7» 처럼 «간부» 가 든 출처 | 볼트 「할 일 추적」 붙여넣기 |
| `who` | «간부 (한민수)» | 손으로 넣은 것 |

- **데스크 회의록은 간부 회의록이 아니다.** 공개가 «간부»(팀장만 봄)여도 종류는 «데스크 전체회의» 다 — 남의 것이 우리 관에 닿는 것일 뿐.
- 회의록이 아직 안 올라온 `from` 은 id 글자로 본다 (id 가 곧 제목이다). 회의록을 못 읽은 채로 화면이 비면 안 된다.
- 자기 앞으로 적은 개인 할 일(`own`)은 안 들어간다.

### 화면

- 머리에 **다음 회의** — `EXEC_MEETINGS` (월 14:00 · 금 15:00, 9/7 간부회의 결정 · 운영 캘린더). 오늘이 회의 요일이면 «오늘».
  워크샵·추석 주 조정은 안 안다 — «다음이 언제인가» 를 셈하는 데만 쓴다.
- 할 일을 **다음 회의 날짜로 가른다**(`execSplit`): 기한 지난 것 · 이번 회의까지(회의 당일 포함) · 그 뒤 · 기한 없는 것(«9월» 같은 글자 기한도 여기) · 끝낸 것(접힘).
- **가장 최근 간부 회의록**의 «정해야 할 것» 과 «다음 회의로» 를 그대로 든다(`execCarry` — `mdSection`). 다음 회의 머리에 올릴 것이 그것이다.
  팀회의가 더 새것이어도 안 고른다.
- 줄을 누르면 할 일 목록의 그 줄로 간다(`gotoTask`). «완료 숨김» 상태면 끝난 줄은 못 찾고 목록만 연다.
- 회의록은 이 메뉴를 열 때도 읽는다(`showPage` 의 조건에 `exec`). 지켜보기(`rebuildTasks`·`marks`)가 다시 그린다.

시험 `tools/test-exec.js` 43건.

## 업무보고 (2026-09-11 초안 — `report-draft` 브랜치, **아직 안 올림**)

"고등부 선생님들이 그날그날 어떤 수업을 했는지와 어떤 업무를 했는지 보고할 수 있도록."
마왕님 결정: 수업은 **학생 단위**(명단 전원 + 옆에 특이사항) · 일일테스트·수업준비는 안 넣음 ·
마감 없음, **다음 날 아침 8시**가 지나면 알림(본인·팀장 둘 다) · 받는 사람은 **원장님**(부원장님 안 거침) ·
**선생님끼리 서로 못 봄**(자기 것과 팀장만).

`dailyReports/{tid}/days/{date}` — 사람마다 칸 하나. 규칙이 선생님 읽기를 `tid == myTid()` 로 묶는다.
⚠ 선생님 읽기에 조건 없는 `team()` 을 쓰면 서로의 보고가 다 열린다. 쓰기는 팀장도 **자기 칸만**.

- **명단은 앱 규칙 그대로** — 정규반은 반 요일에 명단 전원, 개별진도반만 학생 요일(`rpClassesOn`·`rpStudentsOn`). 쉬는 날(`holidayOf`)은 수업 없음
- 판을 열 때 수업관리 앱에서 출석·상담·보강을 읽어 **미리 채운다**(`rpLoadApp`→`rpApplyApp`). 못 읽으면 비운다(결석으로 적지 않는다). 선생님이 고른 출결은 안 덮는다
- 할 일 = 그 날까지 기한인 내 할 일 + 그 날 끝낸 것 + 그 요일 매주 일(`rpTasksFor`). 보고에서 «완료» 로 고르면 내 완료 표시(`toggleMyMark`)도 남는다
- **막는 것은 «내일 할 일» 하나**(`rpCheck`). 출결 미선택·결석 연락 안 함·진도 빔은 알려만 준다
- 알림 = 맨 위 빨간 띠 + 메뉴 숫자(`rpBannerHtml`·`rpOverdue`, 최근 14일). **앱을 열었을 때만** 뜬다 — 휴대폰 푸시가 아니다
- 팀장 «받은 보고»: 낸 사람/수업한 사람, 안 낸 사람, 결석 연락 안 된 학생, 요청, **«원장님께 올릴 문서»**(마크다운 `rpDigest` — 출석이고 적은 것 없는 학생은 뺀다)
- 8시 전에 열면 어제 보고를 먼저 편다(`rpDefaultDate`)

⚠ 함수 이름을 `saveReport` 로 지었다가 «한 줄 보고» 의 `saveReport(id, text)` 와 겹쳐 **나중 것이 조용히 덮었다.**
`saveDailyReport` 로 바꾸고 `test-wiring.js` 에 «같은 이름의 함수가 둘» 검사를 넣었다.

**올릴 때:** `RP_START` 를 올리는 날로 바꾼다(그 전 날들이 전부 «안 냄» 으로 뜬다) → `node tools/publish-rules.mjs` → main 에 합친다.

시험 `tools/test-dailyreport.js` 73건.

## 개발 현황 (2026-09-05)

선생님들이 각자 만든 앱에 **무엇을 넣고 무엇을 고쳤는지**를 한 화면에 모은다.
9/3 전체회의 결정 "각자 만든 도구 공개·공유·통합"의 실행안. 지금은 내 앱 둘(`climath-class`·`climath-team`)로 시작한다.

**근거는 두 갈래다.** 깃허브에 올린 앱은 커밋 이력, 나스 허브만 쓰는 앱은 **일지의 «개발 [앱]» 줄**(아래).
로컬에만 있고 일지도 안 쓰면 볼 방법이 없다.

```
dash/config.repos[]   { key, app, who, owner, repo, url, note, tool, alias }   ← 화면 «저장소 관리»에서 더한다. 코드를 안 고친다
                      tool = 잇는 도구보고 id · alias = 일지에서 이 앱을 부르는 이름(쉼표로 여럿)
                      owner/repo 가 비면 «저장소 주소 없음» — 카드는 뜨되 깃허브는 안 부른다
```

- **다른 선생님 앱은 주소를 받으면 그때 넣는다** (2026-09-05 결정). 처음엔 9/3 시연 도구 넷을 주소 없이
  미리 앉혀 뒀는데 "아직 안 받은 앱을 왜 넣었냐"고 해서 뺐다. 어떤 앱이 있는지는 볼트
  `40 팀장업무/팀 도구 목록.md` 가 들고 있다. 주소 없는 줄(pending)은 여전히 **넣을 수는 있다** —
  카드에 «저장소 주소 없음»이 빨갛게 뜨고 메뉴 숫자도 빨갛다. 기본 목록은 `config.repos` 가 없을 때만 쓴다.
- 담당 이름은 `<datalist>` 로 앱 선생님 명단(`S.teachers`)과 이미 적힌 이름을 내민다. 표기가 갈리면
  사람 거르기가 둘로 쪼개진다. 팀 밖 사람(김효상·김재헌)은 명단에 없어 손으로 친다.
- 열쇠(`key`)는 주소가 있으면 `owner__repo`, 없으면 원래 것을 지킨다. 저장할 때마다 바뀌면 앱 거르기가 풀린다.
- 서버 `api/github.js` 가 저장소마다 최근 200건(100 × 2쪽)과 마지막 deployment 를 받아온다. owner 만.
  브라우저가 api.github.com 을 바로 불러도 되지만 **비공개 저장소의 토큰을 브라우저에 둘 수 없어서** 서버를 거친다.
- 토큰 없이는 IP 당 시간당 60번이다. 서버에 **5분 캐시**를 두고, 화면도 «다시 읽기» 전에는 다시 안 부른다.
  걸리면 몇 분 뒤에 풀리는지 화면에 나온다. `GITHUB_TOKEN`(fine-grained, Contents:read)을 넣으면 5000번.
- 저장소마다 **따로** 부른다(`loadDev`). 하나가 막혀도(비공개·한도) 나머지는 뜬다. 막힌 것은 카드에 이유가 나온다.
- 커밋 한 줄을 `devKind` 가 **기능·고침·문서·손질**로 가른다. 순서가 우선순위다(문서 > 고침 > 손질 > 기능).
  한국어 표지(막히·안 되·되게·던 것…)와 영어(fix/docs)를 본다. 기본값은 기능.

> [!warning] 분류는 커밋 메시지만큼만 맞는다
> "Update index.html" 은 전부 기능으로 간다. 이 저장소들은 한 줄을 제대로 쓰고 있어 잘 갈리지만
> 다른 선생님 저장소는 다를 수 있다. **자동 분류와 함께 커밋 한 줄 규칙을 공유 자리에서 정해야 한다.**
> `못 …` 은 `못 …던` 일 때만 고침이다 — "못 찾은 이유를 보여준다"는 기능인데 처음엔 고침으로 갈렸다.

- "제목 — 덧붙임" 꼴이면 제목만 진하게, 덧붙임은 흐리게(`devSplit`).
- 카드에 **마지막 배포**가 마지막 커밋과 다르면 노란 표시 — 푸시했는데 배포가 안 된 것을 잡는다.
  climath-class 의 deployment 는 `github-pages` 로 찍힌다(버셀이 아니라 Pages 연동이 기록을 남긴다). 날짜와 sha 만 본다.
- 이 메뉴도 회의록처럼 **처음 열 때만** 받아온다(`showPage`).

### 나스 쪽 — 도구보고·일지 (2026-09-05)

팀은 기본적으로 나스 공용폴더(`클라이매쓰AI업무`, 원장 저장소를 robocopy 로 내보낸 것)를 허브로 쓴다.
**거기엔 커밋 기록이 없다.** 사람이 써 둔 것 둘을 읽는다.

```
개발노트/도구보고/날짜_이름_도구.md   → devtools/{파일이름}   파일 하나 = 앱 하나 (원장이 정한 «선생님이 올리는 유일한 곳»)
업무기록/2026/날짜_이름.md            → devlog/{지문}         «- 개발 [앱] 무엇» 꼴의 줄만
```

```bash
node tools/push-devlog.mjs --dry    # 무엇이 올라갈지 (열쇠 없이도)
node tools/push-devlog.mjs          # 올리고, 일지에서 지운 줄은 대시보드에서도 지운다
```

- 브라우저는 나스를 못 본다. 회의록과 같은 방식 — 내 PC 의 스크립트가 올리고 대시보드는 읽기만.
  열쇠는 push-minutes 와 같은 서비스 계정 파일. **나스 드라이브 글자는 PC 마다 다르다**(허브 문서는 Z:, 이 PC 는 RaiDrive 로 U:) — `findHub()` 가 Z·U·X… 를 차례로 본다. `NAS_HUB` 로 못 박아도 된다.
- **일지 전체를 올리지 않는다.** 상담·행정이 섞여 있고 학생ID 가 적힌 줄이 따라온다. `개발 [앱]` 표시가 있는 줄만.
  이 표시는 허브 규칙(원장 관리)에 넣어야 한다 → 볼트 `40 팀장업무/개발 줄 규칙 제안.md` (승인 대기).
- `devAssemble(results, tools, logs)` 가 셋을 카드로 합친다 — **순수 함수**라 시험이 그대로 부른다.
  - 도구보고는 설정의 `tool` 로 깃허브 카드에 잇거나, 같은 사람·같은 앱 이름이면 잇는다. 못 이으면 «나스로만» 카드
  - 일지 줄은 **같은 사람의** 카드 중 앱 이름이 맞는 데 붙는다. 이름 대조는 앱 이름·저장소 이름·별칭·도구보고의 도구 이름·
    **도구보고 파일 이름 꼬리**(`2026-09-04_한민수_학생앱` → `학생앱`)를 본다(`cardMatchesApp`, 띄어쓰기·기호 무시).
    아무 데도 안 맞으면 그 이름으로 «나스로만» 카드가 생긴다 — 도구보고 없이 일지만 써도 보인다
  - 나스 줄은 커밋 링크 대신 일지 파일 이름이 붙는다. 종류 분류는 깃허브 줄과 같은 `devKind`

> [!warning] 다른 사람의 같은 별칭에 붙이면 안 된다
> "학생앱" 이라는 이름은 여러 선생님이 쓸 수 있다. 사람을 먼저 맞추고 앱 이름을 본다.

### 시험

```bash
node tools/test-dev.js
node tools/test-devlog.mjs
```

59건 + 15건. **진짜 커밋 메시지 20건**으로 분류를 보고, 주소 읽기·기간 세기·저장소 목록 저장(`repos` 만 merge, 근거 자료 저장이 이걸 안 지우는지)을 본다.
