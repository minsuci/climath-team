# climath-team — 한민수의 대시보드

대치 클라이매쓰 고등부 교무팀장(한민수)이 **혼자** 쓰는 화면. 수업관리 앱(`climath-class`)과
**별개의 앱**이다. 저장소·Vercel·Firebase 프로젝트가 따로 있고, 앱 DB는 **읽기만** 한다.

```
repo/
├── index.html        # 대시보드 전체. 순수 JS, 빌드 없음
├── api/auth.js       # 로그인 — 수업관리 앱 /api/auth 에 PIN 확인을 맡기고 토큰 둘을 준다
├── api/sheets.js     # 구글시트 읽기 (서비스 계정, 읽기 전용)
├── api/_google.js    # 커스텀 토큰 발급 · ID 토큰 검증 · 구글 API 토큰
├── firestore.rules   # climath-team DB 규칙 — owner 만. 콘솔에 붙여넣어 게시
└── vercel.json       # icn1
```

- 배포: https://climath-team1.vercel.app — Vercel 프로젝트 `climath-team1` (GitHub `minsuci/climath-team` main → 자동 배포)
- Firebase: `climath-team` (Firestore 서울, Authentication은 커스텀 토큰만 — 익명 켜지 말 것)
- 환경변수: `TEAM_SERVICE_ACCOUNT` = climath-team 서비스 계정 JSON 한 줄. Production/Preview/Development 셋 다.
  선택: `CLASS_API_URL` (기본 `https://climath-class.vercel.app/api/auth`)

## 구조 — Firebase 프로젝트 둘

| | 프로젝트 | 무엇 | 쓰기 |
|---|---|---|---|
| `firebase.app()` 기본 | climath-class | teachers · classes · appConfig · exams · scores | 읽기만 |
| 〃 | 〃 | **students · classes.roster** | **쓴다** — 학생 명단이 이 앱에서 관리된다 |
| `teamApp` | climath-team | `dash/tasks`(할 일) · `dash/config`(시트) · `dash/students`(메모) | 여기만 |

브라우저가 두 프로젝트에 따로 로그인한다. `/api/auth login` 이 `classToken`(수업관리 앱이 발급)과
`teamToken`(이 프로젝트 서비스 계정이 서명)을 함께 주고, 각각 `signInWithCustomToken` 한다.
**둘 다 첫 `onAuthStateChanged`를 준 뒤에만 `boot()`** — 한쪽만 복구된 순간 로그인 화면이 깜빡인다.

> [!warning] 로그인하는 동안 boot 을 막아야 한다 (`LOGGING_IN`)
> 두 프로젝트에 차례로 로그인하는데, **첫 번째가 끝나면 그 자리에서 `onAuthStateChanged` 가 돈다.**
> 그때 `boot()` 이 돌면 "한쪽만 로그인됨"으로 보고 `signOutAll()` 로 방금 된 로그인을 도로 끊는다.
> 화면은 오류 없이 로그인 창으로 되돌아가서, 무엇이 틀렸는지 알 길이 없다 (2026-09-04에 이걸로 막혔다).
> 실패 메시지도 전역 `LOGIN_ERR` 에 둔다 — 실패 직후의 signOut 이 `renderLogin` 을 한 번 더 돌려
> 인자로 넘긴 메시지를 지운다.

PIN 대조·시도 제한·선생님 명단은 수업관리 앱에만 있다. 이 앱은 그 서비스 계정 키를 갖지 않는다.
앱이 `role: "owner"` 라고 답할 때만 teamToken 을 발급한다.

## 학생 명단 — 이 앱이 근거지 (2026-09-04)

만들기·고치기·반 배정·지우기를 여기서 한다. **앱에서도 같은 일을 할 수 있다** —
저장되는 곳이 한 군데(앱 `students` + 반 `roster`)라 어느 쪽에서 고쳐도 같은 것이 바뀐다.

**근거지라는 말은 저장소가 여기라는 뜻이 아니다.** 어디서 무엇이 바뀌었는지를
여기서 다 본다는 뜻이다 — 명단 편집의 «최근 바뀐 것»이 그것이고,
앱에서 고친 것도 함께 뜬다 (`appConfig/studentLog`, 앱 DB).
저장소를 이 앱으로 옮기면 학생 로그인·출결·참여표가 전부 여기 매달린다. 그래서 안 옮겼다.

> [!warning] 사본을 여기 두지 않는다
> "여기서 만들고 앱으로 보낸다"를 **두 벌**로 만들면 주인이 둘이 된다. 다른 선생님이 앱에서 넣은
> 학생이 이쪽에 안 보이고 다음 보내기에 조용히 덮인다. 학생 로그인·출결·참여표가 전부
> climath-class 를 보므로 진실은 거기 하나여야 한다. **이 앱은 고치는 자리이고 저장은 앱 DB 한 곳.**
> 앱에 없는 칸(메모)만 `dash/students` 에 붙인다.

관리자 토큰이면 앱 규칙이 이미 `students` 쓰기와 `classes` 갱신을 허용한다 — **앱 규칙은 안 건드렸다.**

- `stSave/stCreate/stDelete` — `students/{pid}`. **지우기는 반 명단에서 먼저 빼고** 사람 문서를 지운다.
  > [!warning] 사람만 지우면 유령이 남는다
  > 앱의 `deleteStudent` 는 사람 문서만 지운다. 앱에서 만든 옛 항목은 `pid` 가 없어서 그래도
  > 명단에 그대로 남아 동작했다. 그런데 **여기서 만든 학생은 `pid` 가 있다.** 사람만 지우면
  > 지워진 사람을 가리키는 항목이 반에 남아 **배치표에는 뜨는데 명단에는 없고**,
  > `rosterDrift`(사람이 없어 건너뜀)도 `looseRosterRows`(pid 가 있어 건너뜀)도 못 잡는다.
  > 2026-09-04에 이걸로 유령이 하나 생겼다. `orphanRosterRows`/`removeOrphans` 가 뒤처리한다.
- `orphanRosterRows()` / `removeOrphans()` — 지워진 사람을 가리키는 반 항목. 편집 화면 배너에서 뺀다.
  배치표에서는 빨간 취소선으로 뜬다.
- `stSyncRosters(person)` — 이름·학교·학년을 그 사람이 든 모든 반의 `roster` 미러에 흘려보낸다.
  **이름을 빼먹으면** 출석부·참여표가 옛 이름을 계속 쓴다.
- `withFreshRoster(cid, fn)` — 반 명단은 배열 하나라 통째로 다시 쓴다. 그래서 **쓰기 직전에 반 문서를
  다시 읽고** 그 위에 얹는다. 화면이 들고 있던 사본으로 쓰면 그 사이 다른 선생님이 넣은 학생이 날아간다.
- `rosterDrift()` / `fixDrift()` — 미러가 어긋난 것을 찾아 다시 보낸다. 편집 화면 위 배너.
- `looseRosterRows()` / `linkLoose()` — 반에만 있고 사람 정보가 없는 옛 항목. **이름이 하나뿐일 때만**
  잇는다. 겹치면 건너뛴다 — 동명이인을 섞으면 되돌리기 어렵다.

### 시험

```bash
node tools/test-roster.js
```

가짜 Firestore로 쓰기 로직을 돌린다(24건). **운영 DB를 건드리는 코드라 고치면 반드시 돌린다.**
"남이 그 사이 넣은 학생이 안 지워지는가", "지우고 나서 유령이 안 남는가"까지 본다 — 눈으로는 못 잡는다.
가짜 Firestore 는 쓸 때 **반드시 직렬화(깊은 복사)해야 한다.** 참조를 그대로 두면 화면 배열과
서버 배열이 같은 것이 되어 없는 버그가 보인다(2026-09-04에 한 번 헛짚었다).

## 학교 일정 — **이 앱에서만 관리한다** (2026-09-04)

수업관리 앱에서 학교 일정 화면을 지웠다. 채우는 일은 교무팀장이 여기서 하고,
선생님들은 앱에서 참여표만 본다. 화면이 두 벌이면 어느 쪽이 맞는지 아무도 모른다.

앱에 남은 것은 읽는 쪽뿐이다(학생 화면 미리 채우기 · 일정 갈림 잡기).
서버 `api/schedule.js` 도 앱에 그대로 있고 여기서 중계해서 쓴다 — 로직은 한 벌이다.

**참여표의 «학교 일정으로 채우기» 도 여기로 옮겨왔다** (`fillTargets`·`runFill`).
일정을 여기서만 채우는데 옮기는 단추가 앱에 있으면 반쪽만 옮긴 것이 된다.

## 시험 기간 찾아오기

학교·학년마다 내신 기간과 수학시험 날짜를 한 번만 적어두는 곳. 수업관리 앱의 `SchoolTermBoard` 와
**같은 문서**(`appConfig/schoolTerms`)를 쓴다. 참여표가 여기서 기본값을 가져간다.
표의 날짜 칸은 여기서 바로 고친다(`saveSchoolTerm` — **넘긴 칸만** 보낸다).

**찾아오기는 앱의 `/api/schedule` 을 중계한다**(`api/schedule.js`). 나이스 → 학교 홈페이지 →
게시판 첨부문서(→ AI로 날짜 뽑기) 네 단계가 이미 그쪽에 쌓여 있다. 여기서 다시 만들면 두 벌이 갈린다 —
학교마다 시험을 부르는 말이 다른 규칙이 거기 모여 있다.

- 브라우저가 직접 못 부른다(그쪽에 CORS 헤더가 없다). 앱을 안 고치기로 했으므로 서버끼리 부른다.
- 검사가 두 겹이다 — 여기서 team 토큰이 owner 인지, 저쪽에서 class 토큰이 선생님인지.
  그래서 브라우저가 토큰 둘을 함께 보낸다.
- 학교 하나가 문서까지 가면 20~30초. 중계 함수에 `maxDuration = 60` 을 준다.
- **넷씩 나란히** 부른다(`PULL_CONC`). 한 줄로 세우면 느린 학교가 곧 전체 시간이 된다.
- 작업 상태는 **모듈(`PULL_JOB`)에 둔다.** 화면에 매어두면 다른 메뉴를 누르는 순간 통째로 날아간다.

> [!warning] 이미 적어둔 값을 덮지 않는다
> 학교가 날짜를 바꿨을 수도, 사람이 맞게 고쳤을 수도 있다. 다르면 **알려주고 고르게** 한다.
> 날짜가 같은데 이름이 옛 나이스 이름이면 **이름만** 고친다 — 이미 셋(중간고사·기말고사·졸업고사)
> 중 하나면 손대지 않는다(사람이 적은 것을 덮지 않으려고).
> 시험을 못 찾아도 **학기 계획과 근거 링크는 적어둔다** — "이번 회차에 시험이 없다"와
> "못 찾았다"를 가르는 유일한 단서이고, 손으로 채울 때 바로 필요하다.

**수학 시험 날짜는 학사일정에 없다.** 과목별 시간표는 안 올라온다. 손으로 넣거나 학생이 낸다.

앱 쪽 파이프라인에 **네 번째 길**이 있다(2026-09-04) — 메뉴 본문에 그림으로 붙여둔 학사일정.
그림에서 읽은 학교는 `via: "image"` 로 오고 결과 줄에 `(그림)` 으로 표시된다. **사람이 한 번 봐야 한다.**
→ 볼트 `30 노하우/학사일정을 그림으로 붙여둔 학교가 있다.md`

### 시험

```bash
node tools/test-terms.js
```

20건. 저장 열쇠(앱과 같아야 한다), 학기 계획 줄, 줄 모으기, 그리고 채움/덮지 않음/이름만 고침 판단.

## 내신 참여표 — 여기서 만든다 (2026-09-04)

수업관리 앱의 `ExamBoard`/`ExamEntry` 와 **같은 자리에 같은 모양으로** 쓴다. 그래서 여기서 만든 것이
앱 참여표에 그대로 뜨고, 학생이 낸 것도 여기 그대로 보인다.

```
classes/{cid}/exams/{회차키}__{sid}    회차키 = 회차의 / 와 공백을 _ 로 바꾼 것
appConfig/examTerm                     회차는 학원 전체로 하나
```

> [!warning] 문서 열쇠가 앱과 한 글자라도 다르면 서로 다른 문서에 쓴다
> `examKey(term, sid) = String(term).replace(/[\/\s]+/g, "_") + "__" + sid`.
> 조회는 `where("term","==",회차)` 라 문서에 `term` 필드도 **원문 그대로** 들어가야 한다.
> `tools/test-exam.js` 첫 두 건이 이걸 지킨다.

- **본체는 표가 아니라 날짜 띠다.** 이 표가 답하는 질문이 "누가 언제 안 오고 언제 돌아오나"라서다
  (원본 구글시트가 그랬다). 띠 = 시험 있는 달 1일 ~ 가장 늦은 복귀일. 표식은 `직` `수` `시` `첫`.
- **대신 입력은 날짜 칸이 아니라 달력에 칠한다**(`PAINT_MODES`). 시험기간은 시작·끝 두 번 누른다.
  수학시험을 정하면 직보가 전날로 따라가되 **손으로 고쳐둔 직보는 놔둔다.**
- 반 선택 없이 **담당 반을 가로질러** 모은다. 반별로 보면 "이번 주에 누가 빠지나"를 못 본다.
- 학교는 목록에서 고른다(`KNOWN_SCHOOLS` + 학생·학교일정에 있는 이름). 자유 입력이면
  중동고/중동고등학교/중동이 섞여 **같은 학교인지 판별 자체가 안 된다.**
- 같은 학교·학년인데 `examSig`(시작·끝·수학)가 갈리면 띄우기만 한다. **자동으로 안 고친다** —
  분반마다 진짜 다를 수 있고 무엇이 맞는지는 사람이 안다.
- 대신 입력을 새로 열 때 `appConfig/schoolTerms` 에 적어둔 학교 일정으로 **빈 칸을 미리 채운다.**
- 복귀일은 표에서 바로 넣는다(선생님이 정하는 값이다).
- **정렬** — 이름·반·담임·학교·내신기간·수학·등원. 위 칩이나 열 머리를 누르면
  오름 → 내림 → 원래 순서로 돈다. **빈 칸은 방향과 상관없이 늘 맨 아래** — 안 채운 사람을
  찾으려고 정렬하는 게 아니다. 같은 값이면 이름 가나다.
  > [!warning] 띠와 표에 정렬을 따로 걸면 안 된다
  > 둘이 같은 배열을 써야 한다. 따로 걸면 **이름은 같은 줄에 있는데 날짜 칸이 다른 사람 것**이 된다.
  > `renderExams` 가 `examSortRows` 를 한 번만 부르고, 띠는 그 결과에서 낸 사람만 걸러 쓴다.
  > 등원 회차 정렬은 숫자를 세 자리로 채워 비교한다 — 글자로 세면 10이 9보다 앞에 온다.

### 등원 회차 — 수강료에 쓰는 숫자

```
등원 = 그 달 수업일 − (직보 다음날 ~ 복귀 전날) + 직보·복귀
```

> [!warning] 세 가지를 틀리기 쉽다
> 1. **안 오는 구간을 내신기간(start~end)으로 재면 안 된다.** 직보가 마지막 수업이고 그 다음 오는
>    날이 복귀라, 시험이 끝난 뒤라도 복귀 전이면 안 온다. 화목반이 9/23에 끝나고 9/29에 복귀하면
>    9/24(목)은 안 오는데 내신기간으로 재면 오는 날로 잡힌다. `outRange()` 가 한 곳에서 정한다.
> 2. **요일을 안 보는 판정을 쓰면 그 달 전체가 수업일이 되어 31회로 나온다.** `isClassDay()` 를 쓴다.
> 3. **띠 범위와 회차 세는 범위가 다르다.** 띠는 복귀일에서 끝나지만 회차는 **그 달 전체**로 센다.
>    복귀가 25일이어도 26~31일 수업일은 수강료에 들어간다.

제출에 `days`(수업 요일)를 함께 저장한다 — 나중에 반 요일이 바뀌어도 그때 센 회차가 재현된다.

### 시험

```bash
node tools/test-exam.js
```

39건. 위 함정과 정렬을 전부 시험한다. 계산을 고치면 반드시 돌린다.

## 시험 성적 — 중간·기말·모의 (2026-09-04)

메뉴 이름이 "성적 입력"이었을 때는 학생이 낸 **일일 테스트**를 세기만 했다. 팀장이 보고 싶은 건
그게 아니라 **중간고사·기말고사·모의고사**라서 다시 짰다.

앱에 없는 자료라 **이 앱 DB(climath-team)** 에 둔다. 앱 규칙을 안 건드린다.

```
tests/{tid}                { kind: midterm|final|mock, name, grade, date, created }
testScores/{tid}__{pid}    { tid, pid, name, raw, ach, grade, std, pct, avg, note, updated }
```

- 시험 종류마다 칸이 다르다 — 중간·기말은 `원점수 · 성취도(A~E) · 등급 · 과목평균`,
  모의고사는 `원점수 · 표준점수 · 백분위 · 등급`. `TEST_KINDS[kind].cols` 하나만 보면 된다.
- 표는 **그 학년 학생 전원**이 뜬다(`testRoster`). 안 본 사람도 자리가 있어야 누가 빠졌는지 보인다.
- 붙여넣기(`parseScoreLine`)는 `이름` 다음에 그 종류의 숫자 칸 차례. **성취도(A~E)는 자리를 안 가린다**
  (글자라 숫자와 안 섞인다). `-` 는 빈 칸. 이름이 겹치면 넣지 않고 알려준다.
- 요약은 등급 분포 + **담임별·반별 평균**. 팀장이 보는 화면이라 사람별이 아니라 담당별로 묶는다.
- 정렬은 이름·반·담임·학교와 점수 칸 전부. 점수는 처음 누르면 **내림차순**(잘한 사람부터가 자연스럽다),
  이름·반은 오름차순. 빈 칸은 늘 맨 아래.

> [!warning] 값이 전부 비면 문서를 지운다
> 빈 껍데기를 남기면 "응시함"으로 세어져 **평균과 인원이 틀어진다.** `saveScore` 가 확인해 지운다.

> [!warning] 시험을 지우면 성적도 같이 지운다
> 시험만 지우면 어디에도 안 보이는 성적이 남는다 — 학생 지우기에서 유령이 생긴 것과 같은 함정이다.
> `deleteTest` 가 `testScores` 를 먼저 훑어 지운다.

일일 테스트 현황은 없애지 않고 맨 아래 접어 뒀다 — "이 반은 테스트를 안 돌리고 있다"는 신호는
따로 쓸모가 있어서다. 자료가 다르다는 것을 그 자리에 적어 뒀다.

### 근거 시트에서 가져오기

성적을 따로 적는 구글시트가 있다. 시험을 고르고 **시트에서 가져오기** 를 누르면 원점수·예상등급을 끌어온다.
시트 주소는 `dash/config.scoreSheet` 에 두고 화면에서 고친다 — **공개 저장소라 코드에 안 적는다**(볼트에 있다).

시트 생김새 — 머리글이 **두 줄**이고 시험별로 칸이 묶여 있다.

```
1줄  연번 | 이름 | 학교/학년 | 소속 | 담임 | 3월 모의고사 ←─묶임─→ | 2026-1학기 중간고사 ←→ | …
2줄                                       수학(원점수) | 예상등급 | 비고 | 수학(예상등급) | 비고
```

> [!warning] 묶인 칸은 맨 왼쪽에만 값이 온다
> 구글 시트 API 는 병합 셀의 값을 **맨 왼쪽 칸에만** 주고 나머지는 빈 칸으로 준다.
> 드라이브 텍스트 내보내기는 `[merged]` 로 반복해 보여주므로 **그걸 보고 짜면 칸이 밀린다** —
> 점수가 다른 시험 칸에서 딸려온다. `parseScoreSheet` 가 1줄을 오른쪽으로 끌어 채운 뒤(ffill)
> 2줄에 이름표가 있는 칸만 점수 칸으로 본다.

- 탭마다 생김새가 다르다(학교/학년이 한 칸이기도 두 칸이기도, 기말에 과목 칸이 붙기도) — 그래서
  **자리를 못 박지 않고 글자로 찾는다**(`/^이름$/`, `/학교/`, `/원점수/`, `/등급/`).
- 탭 전체를 훑어 (탭 × 시험 묶음)을 모으고, 시험 이름으로 고른다(`matchGroup`, 띄어쓰기 무시).
  정확히 안 맞으면 비슷한 것을 고르되 **그렇다고 화면에 적는다.** 목록에서 직접 바꿀 수 있다.
- 학생은 **이름으로** 잇고, 이름이 겹치면 시트의 학교로 가른다. 못 가르면 넣지 않고 이유를 보여준다.
- 미리보기를 먼저 보여준다. **이미 넣은 값은 기본으로 안 덮는다**(체크로 덮어쓰기).
- 시트는 **읽기만** 한다.

### 시험

```bash
node tools/test-scores.js
node tools/test-sheet.js
```

각각 26건. 붙여넣기 해석·평균·등급 색·빈 값 지우기·시험과 성적 함께 지우기,
그리고 **실제 시트의 머리글 모양 그대로** 놓고 묶임 풀기·칸 찾기·학생 잇기.

## 학년 업무 달력 (2026-09-04)

원장님이 8/31 간부회의에서 지시한 그것 — *"이번 주에 요일별로 뭐 해야 되고 뭐 보고해야 돼"*.
그래서 **달력이 본체가 아니라 「이번 주까지 해야 할 일」이 본체**다. 달력은 그걸 요일에 늘어놓은 것이라
목록을 위에, 달력을 아래에 둔다.

> [!warning] 할 일을 두 벌로 나누지 않는다
> 팀 할 일과 **같은 곳**(`dash/tasks`)을 쓴다. 나누면 어느 쪽이 맞는지 알 수 없다 —
> 학생 명단에서 배운 것과 같다. 여기서 더 쓰는 칸은 `grade`(학년)와 `repeat`(반복)뿐이다.

```
한 번 하는 일 : { due, status }
반복하는 일   : { repeat: { dow: [1,5] }, doneOn: { "2026-09-05": true } }
```

> [!warning] 반복은 날짜마다 따로 끝난다
> `status` 하나로 다루면 **이번 주에 했다고 다음 주도 끝난 게 된다.** `doneOn` 에 날짜를 적는다.
> `taskDoneOn()` 이 한 곳에서 가른다 — 반복이면 `status` 를 아예 안 본다.

> [!warning] 주는 월요일에 시작한다 — 일요일이 함정
> `getDay()` 가 일요일에 0 이라 그냥 빼면 **다음 주 월요일**이 나온다. `weekStart()` 가 0 을 따로 본다.

- 학년 탭은 `전체 / 중3 / 고1 / 고2 / 고3 / 공통`. **학년이 없는 할 일은 「공통」**이고, 특정 학년을
  고르면 안 나온다(전체에서는 나온다). 안 그러면 학년 달력이 공통 업무로 뒤덮인다.
- 손으로 안 넣어도 뜨는 것(`calAuto`) — 학교 시험 날짜(그 회차만), 시험 성적의 시험 날짜, 반 종강.
  이건 **일정이지 할 일이 아니라** 체크가 안 붙는다. 회색·노랑·빨강 칩으로만 뜬다.
- 날짜 칸의 `＋` 로 그 날에 바로 넣는다. **매주 ○요일** 을 켜면 반복이 된다.

### 시험

```bash
node tools/test-cal.js
```

32건. 주 계산(일요일 포함), 어느 날에 놓이는가, 반복의 날짜별 완료, 이번 주까지 목록,
학년 거르기, 앱이 아는 일정.

## 함정

- 앱 데이터의 칸 이름은 수업관리 앱 CLAUDE.md 가 원본이다. 특히 학생 명단의 담임은 `students.homeroom`(tid),
  roster 의 `teacher` 는 "이 항목이 선생님"이라는 불리언. 앱이 칸을 바꾸면 여기가 조용히 빈다.
- 상자 일곱 개를 `render*()` 가 각각 innerHTML 로 다시 그린다. 상자를 통째로 다시 그리면 입력 포커스가
  날아가므로 시트 찾기 칸은 표만 다시 그린다(`renderSheetTable`).
- 성적은 반마다 `days` 문서를 모두 나열한 뒤 최근 14개 날짜만 `scores` 를 읽는다. 반이 늘면 느리다.
  앱 쪽에 `collectionGroup("scores")` 규칙을 열면 한 번에 되지만 그건 앱 규칙을 건드리는 일이라 안 했다.
- 시트 id 는 저장소에 적지 않는다(공개). 대시보드 안에서 주소를 붙여넣어 `dash/config` 에 둔다.
- 구글시트는 시트마다 서비스 계정 이메일을 **뷰어**로 공유해야 한다. 처음엔 구글 클라우드 콘솔에서
  climath-team 의 Google Sheets API 를 켜야 한다. `api/sheets.js` 의 `explain()` 이 그 링크를 돌려준다.
- 로컬에서 켜볼 수 없다. `/api` 가 Vercel 에만 있고 Firestore 는 로그인이 있어야 열린다.
  구문 검사: `node --check api/*.js`, index.html 은 `<script>` 를 뽑아 `new vm.Script()`.
  **그리기 함수는 가짜 데이터로 돌려볼 수 있다** — `<script>` 를 vm 컨텍스트에 넣고(firebase·document·
  location 은 빈 껍데기로) `S` 에 반·학생을 손으로 채운 뒤 `classGridHtml()` 같은 걸 불러 HTML 을 본다.
  브라우저 없이 배치표가 실제로 그려지는지 확인하는 유일한 방법이다.

## 볼트

작업 노트: `민수의 뇌/20 작업/한민수 대시보드.md` — 켜는 순서, 연동 후보 시트 목록.
